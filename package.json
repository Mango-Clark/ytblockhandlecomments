{
  "name": "ytblockhandlecomments",
  "private": true,
  "type": "module",
  "scripts": {
    "bump:version": "node scripts/bump-version.ts",
    "build": "node scripts/build-userscript.ts",
    "check:build": "node scripts/build-userscript.ts --check",
    "lint:markdown": "markdownlint \"**/*.md\"",
    "test": "node --test",
    "typecheck": "tsc --noEmit && tsc --noEmit -p scripts/tsconfig.json && tsc --noEmit -p tests/tsconfig.json"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.28.1",
    "eslint": "^10.5.0",
    "globals": "^17.7.0",
    "jiti": "^2.7.0",
    "markdownlint-cli": "^0.47.0",
    "markdownlint-cli": "^0.49.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.0"
  }
}
