// ==UserScript==
// @name         YouTube Comment Blocker
// @namespace    YouTube_Comment_Blocker
// @version      0.5.1
// @description  Block/unblock comment handles via right-click. Optional UID pairing via YouTube Data API, real-time hiding, custom popup, and block list management.
// @homepage     https://github.com/Mango-Clark/ytblockhandlecomments/
// @updateURL    https://raw.githubusercontent.com/Mango-Clark/ytblockhandlecomments/refs/heads/master/ytblockhandlecomments.js
// @downloadURL  https://raw.githubusercontent.com/Mango-Clark/ytblockhandlecomments/refs/heads/master/ytblockhandlecomments.js
// @author       Mango_Clark
// @match        https://www.youtube.com/*
// @grant        GM_getValue
// @grant        GM_setValue
// @grant        GM_addValueChangeListener
// @grant        GM_registerMenuCommand
// @grant        GM_unregisterMenuCommand
// @run-at       document-idle
// ==/UserScript==
(() => {
	'use strict';
	const TEST_HOOK = typeof globalThis === 'object' ? globalThis.__YT_BLOCK_TEST_HOOK__ || null : null;
const style=document.createElement('style');style.textContent=`
    .tm-toast{position:fixed;left:50%;bottom:24px;transform:translateX(-50%);background:#323232;color:#fff;padding:8px 16px;border-radius:6px;opacity:0;transition:opacity .2s ease;z-index:10000;font-size:15px;pointer-events:none}
    .tm-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.4);display:flex;align-items:center;justify-content:center;z-index:10000}
    .tm-dialog{background:#fff;color:#000;padding:24px 28px;border-radius:12px;width:min(980px,94vw);max-width:980px;box-shadow:0 10px 30px rgba(0,0,0,.25);max-height:84vh;display:flex;flex-direction:column;font-size:14px}
    .tm-dialog header{margin:0 0 14px 0;font-size:18px;font-weight:700}
    .tm-dialog .tm-content{flex:1 1 auto;overflow:auto;min-height:0}
    .tm-dialog footer{display:flex;justify-content:flex-end;gap:8px;margin-top:16px;flex-wrap:wrap}
    .tm-dialog button{padding:10px 16px;border:none;border-radius:8px;font-size:14px;cursor:pointer}
    .tm-dialog button.primary{background:#065fd4;color:#fff}
    .tm-dialog button.secondary{background:#eee;color:#000}
    .tm-dialog button[disabled]{opacity:.6;cursor:wait}
    .tm-dialog textarea{width:100%;height:260px;resize:vertical;margin-top:8px;font-family:monospace;font-size:14px}
    .tm-block-list{list-style:none;padding:0;margin:0}
    .tm-block-list li{display:flex;justify-content:space-between;align-items:flex-start;padding:10px 0;gap:12px;word-break:break-word;border-bottom:1px solid #ececec}
    .tm-block-list li:last-child{border-bottom:none}
    .tm-block-list li button{padding:4px 12px;border:none;border-radius:8px;font-size:13px;cursor:pointer;background:#d32f2f;color:#fff;flex:0 0 auto}
    .tm-block-main{display:flex;flex-direction:column;gap:6px;min-width:0;flex:1 1 auto}
    .tm-block-label{font-weight:600;word-break:break-all}
    .tm-block-badges{display:flex;gap:6px;flex-wrap:wrap}
    .tm-badge{display:inline-flex;align-items:center;border-radius:999px;padding:3px 8px;font-size:12px;font-weight:600;background:#f1f3f4;color:#333}
    .tm-badge.handle-only{background:#fff4d6;color:#8a5b00}
    .tm-badge.paired{background:#e3f5ea;color:#1d6f42}
    .tm-badge.stale{background:#ffe5cc;color:#a45100}
    .tm-badge.mismatch{background:#fde2e1;color:#b42318}
    .tm-badge.unverified{background:#ecebff;color:#5243aa}
    .tm-badge.uid{background:#e4f0ff;color:#0b57d0}
    .tm-badge.regex{background:#ececec;color:#444}
    .tm-block-meta{font-size:12px;line-height:1.5;color:#5f6368}
    .tm-section{border:1px solid #e5e5e5;border-radius:12px;padding:14px 16px;margin-bottom:14px}
    .tm-section h3{margin:0 0 10px 0;font-size:15px}
    .tm-toggle-row{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;flex-wrap:wrap}
    .tm-toggle-row label{display:flex;align-items:center;gap:8px;font-weight:600}
    .tm-toggle-row p{margin:6px 0 0 0;font-size:12px;color:#5f6368}
    .tm-inline-actions{display:flex;gap:8px;flex-wrap:wrap}
    .tm-inline-actions button{padding:8px 12px;font-size:13px}
    .tm-summary-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(120px,1fr));gap:8px}
    .tm-summary-card{border-radius:10px;background:#f8f9fa;padding:10px 12px}
    .tm-summary-card strong{display:block;font-size:18px}
    .tm-summary-card span{font-size:12px;color:#5f6368}
    .tm-toolbar{display:flex;flex-direction:column;gap:10px;margin-bottom:12px}
    .tm-toolbar-row{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;flex-wrap:wrap}
    .tm-toolbar-group{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    .tm-toolbar-group label{font-weight:600}
    .tm-toolbar-group select{padding:7px 10px;border:1px solid #d0d7de;border-radius:8px;background:#fff;color:inherit}
    .tm-toolbar-group input[type="search"]{padding:7px 10px;border:1px solid #d0d7de;border-radius:8px;background:#fff;color:inherit;min-width:220px}
    .tm-tag-group{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    .tm-tag-group label{display:inline-flex;align-items:center;gap:6px;font-weight:500}
    .tm-counter{font-size:12px;color:#5f6368}
    .tm-list-empty{padding:18px 0;text-align:center;color:#5f6368}
    .tm-item-check{margin-top:2px}
    .tm-inline-note{font-size:12px;color:#5f6368}
    .tm-result-panel{margin-top:10px;border-top:1px solid #ececec;padding-top:10px}
    .tm-result-panel details{border:1px solid #e5e5e5;border-radius:10px;padding:8px 10px;background:#fafafa}
    .tm-result-panel summary{cursor:pointer;font-weight:600}
    .tm-result-list{list-style:none;padding:0;margin:10px 0 0 0;display:flex;flex-direction:column;gap:8px}
    .tm-result-list li{padding:8px 10px;border:1px solid #ececec;border-radius:8px}
    .tm-result-outcome{font-weight:700}
    .tm-search-note{font-size:12px;color:#5f6368}
    .tm-regex-summary{display:flex;flex-direction:column;gap:8px}
    .tm-regex-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    .tm-regex-match-list{list-style:none;padding:0;margin:0;display:flex;flex-direction:column;gap:6px}
    .tm-regex-match-list li{padding:6px 8px;border:1px solid #ececec;border-radius:8px}
    .tm-regex-bar{position:sticky;top:0;z-index:1;background:#fff;border:1px solid #e5e5e5;border-radius:12px;padding:12px 14px;margin-bottom:14px}
    .tm-regex-bar header{margin:0;font-size:16px;font-weight:700}
    .tm-regex-bar .row{display:flex;align-items:center;justify-content:space-between;gap:8px;flex-wrap:wrap}
    .tm-regex-bar .controls{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    .tm-regex-bar input,.tm-section input{padding:7px 10px;border:1px solid #d0d7de;border-radius:8px}
    .tm-banner{position:fixed;top:18px;right:18px;z-index:9999;max-width:min(420px,calc(100vw - 36px));background:#fff7e6;color:#3d2f00;border:1px solid #ffd27a;border-radius:14px;box-shadow:0 12px 24px rgba(0,0,0,.18);padding:14px 16px}
    .tm-banner strong{display:block;margin-bottom:6px}
    .tm-banner p{margin:0;font-size:13px;line-height:1.45}
    .tm-banner .actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}
    .tm-banner .actions button{padding:8px 12px;border:none;border-radius:8px;cursor:pointer;font-size:13px}
    .tm-banner .actions .primary{background:#065fd4;color:#fff}
    .tm-banner .actions .secondary{background:#fff;color:#3d2f00;border:1px solid #ffd27a}
    .tm-muted{font-size:12px;color:#5f6368}
    .tm-hidden{display:none !important}
    @media (max-width: 640px){
      .tm-dialog{padding:18px}
      .tm-block-list li{flex-direction:column}
      .tm-block-list li button{align-self:flex-end}
      .tm-toolbar-row,.tm-toolbar-group{align-items:stretch}
      .tm-toolbar-group{flex-direction:column;align-items:flex-start}
      .tm-toolbar-group input[type="search"]{min-width:0;width:100%}
      .tm-banner{left:12px;right:12px;top:auto;bottom:72px;max-width:none}
    }
    @media (prefers-color-scheme: dark){
      .tm-dialog{background:#1f1f1f;color:#fff}
      .tm-dialog button.secondary{background:#333;color:#fff}
      .tm-section,.tm-regex-bar{background:#1f1f1f;border-color:#444}
      .tm-summary-card{background:#2a2a2a}
      .tm-block-list li{border-color:#333}
      .tm-block-meta,.tm-toggle-row p,.tm-summary-card span,.tm-muted,.tm-inline-note,.tm-search-note{color:#c7c7c7}
      .tm-counter,.tm-list-empty{color:#c7c7c7}
      .tm-badge{background:#303134;color:#f1f3f4}
      .tm-badge.handle-only{background:#4b3900;color:#ffd76a}
      .tm-badge.paired{background:#143823;color:#87d7a6}
      .tm-badge.stale{background:#4b2c00;color:#ffbe76}
      .tm-badge.mismatch{background:#4a1e1e;color:#ff8a80}
      .tm-badge.unverified{background:#2e2559;color:#c7b9ff}
      .tm-badge.uid{background:#16325c;color:#9bc2ff}
      .tm-banner{background:#2a2416;color:#ffe8ad;border-color:#8e6c25}
      .tm-banner .actions .secondary{background:#2a2416;color:#ffe8ad;border-color:#8e6c25}
      .tm-result-panel details,.tm-regex-match-list li{background:#2a2a2a;border-color:#444}
      .tm-result-list li{border-color:#444}
      .tm-regex-bar input,.tm-section input,.tm-toolbar-group select,.tm-toolbar-group input[type="search"]{background:#111;color:#fff;border-color:#555}
    }
  `;document.head.appendChild(style);const sanitizeHandle=(h)=>{if(!h)return null;h=String(h).trim();if(!h.startsWith('@'))return null;return h;};const norm=(h)=>{const handle=sanitizeHandle(h);return handle?handle.toLowerCase():null;};const getHandleCompareKey=(h,caseSensitive=false)=>{const handle=sanitizeHandle(h);if(!handle)return null;return caseSensitive?handle:handle.toLowerCase();};const getItemKey=(item)=>{if(!item||!item.type)return null;if(item.type==='handle'){const handle=sanitizeHandle(item.value);return handle?`h:${handle}`:null;}if(item.type==='id'){const id=String(item.value||'').trim();return isChannelId(id)?`i:${id}`:null;}return item.type==='regex'?`r:${String(item.value)}/${item.flags || ''}`:null;};const decodeMaybe=(value)=>{try{return decodeURIComponent(value);}catch{return value;}};const findHandleItem=(items,handle,caseSensitive=false)=>{const key=getHandleCompareKey(handle,caseSensitive);if(!key)return null;return(items||[]).find(item=>item?.type==='handle'&&getHandleCompareKey(item.value,caseSensitive)===key)||null;};const isChannelId=(value)=>/^UC[0-9A-Za-z_-]{10,}$/.test(String(value||'').trim());const SAFE_REGEX_MAX_PATTERN=256;const SAFE_REGEX_MAX_TARGET=128;const SAFE_REGEX_MAX_RUNTIME_MS=5;const SAFE_REGEX_FLAGS=/^[gimsuy]*$/;const REGEX_MATCH_INITIAL_LIMIT=20;const REGEX_MATCH_PAGE_SIZE=50;const UNSAFE_REGEX_PATTERNS=[/\((?:\\.|[^()\\])*(?:[+*?]|\{\d*,?\d*\})(?:\\.|[^()\\])*\)(?:[+*?]|\{)/,/\((?:\\.|[^()\\])+\|(?:\\.|[^()\\])+\)(?:[+*?]|\{)/,/(?:\.\*){2,}/,/(?:\[[^\]]+\][+*]){2,}/];const validateRegexSpec=(pattern,flags='')=>{const value=String(pattern||'');const flagText=String(flags||'');if(!value||value.length>SAFE_REGEX_MAX_PATTERN)return null;if(flagText.length>6||!SAFE_REGEX_FLAGS.test(flagText)||new Set(flagText).size!==flagText.length)return null;if(UNSAFE_REGEX_PATTERNS.some(rx=>rx.test(value)))return null;try{new RegExp(value,flagText);return{pattern:value,flags:flagText};}catch{return null;}};const parseRegexLiteral=(text)=>{const s=String(text||'').trim();if(!s.startsWith('/'))return null;let escaped=false;for(let i=s.length-1;i>0;i--){const ch=s[i];if(ch==='/'&&!escaped)return{pattern:s.slice(1,i).replace(/\\\//g,'/'),flags:s.slice(i+1)};escaped=ch==='\\'&&!escaped;if(ch!=='\\')escaped=false;}return null;};const exportRegexLiteral=(item)=>`/${String(item.value || '').replace(/\//g, '\\/')}/${item.flags || ''}`;const safeRegexTest=(rx,value)=>{if(!rx||!value)return false;const target=String(value).slice(0,SAFE_REGEX_MAX_TARGET);const startedAt=performance.now();try{rx.lastIndex=0;const matched=rx.test(target);return(performance.now()-startedAt)<=SAFE_REGEX_MAX_RUNTIME_MS&&matched;}catch{return false;}};const COMMENT_SELECTOR='ytd-comment-thread-renderer, ytd-comment-renderer, ytd-comment-view-model';const COMMENTS_HOST_SELECTOR='ytd-comments#comments, ytd-comments';const WATCH_ROOT_SELECTOR='ytd-watch-flexy, ytd-watch-grid, ytd-page-manager';const SHORTS_ROOT_SELECTOR='ytd-reel-video-renderer, ytd-shorts, ytd-app, ytd-page-manager';const PAIR_STALE_MS=7*24*60*60*1000;const PAIR_NOTICE_COOLDOWN_MS=24*60*60*1000;const FALLBACK_SCRIPT_VERSION='0.5.1';const getScriptVersion=()=>{try{if(typeof GM_info==='object'&&GM_info?.script?.version)return GM_info.script.version;}catch{}return FALLBACK_SCRIPT_VERSION;};const I18N={ko:{block:'차단',unblock:'차단 해제',confirmBlock:'🛑 차단할까요?',confirmUnblock:'❌ 차단을 해제할까요?',manageTitle:(n)=>`차단된 채널 (${n})`,import:'가져오기',export:'내보내기',close:'닫기',removed:(h)=>`${h} 해제`,added:(h)=>`${h} 차단`,clear:'초기화',confirmClear:'모든 차단 핸들을 초기화할까요?',menuManage:'🔍 차단 목록 관리',menuClear:'🗑️ 차단 목록 초기화',versionTitle:'스크립트 정보',versionValue:(version)=>`버전: ${version}`,syncToast:'차단 목록이 다른 탭과 동기화되었습니다.',pairSyncToast:'UID pair 설정이 다른 탭과 동기화되었습니다.',exportHint:'JSON 또는 라인별 텍스트로 복사하세요.',json:'JSON',text:'텍스트',importTitle:'차단 목록 가져오기',importPlaceholder:'JSON 또는 @handle/정규식 라인별/쉼표 구분',importBtn:'가져오기',importedCount:(n)=>`${n}개 항목 가져옴`,menuHide:'이 채널의 댓글 숨김',menuUnhide:'이 채널 댓글 숨김 해제',addRegex:'정규식 추가',patternLabel:'패턴',flagsLabel:'플래그',addBtn:'추가',invalidRegex:'유효하지 않은 정규식',addedRegex:'정규식을 추가했습니다',exists:'이미 존재합니다',testRegex:'정규식 만들기/테스트',uidDetectionLabel:'UID 기반 감지',uidDetectionHelp:'핸들 차단은 항상 유지하고, UID 일치 검사만 추가로 켭니다.',apiKeyTitle:'YouTube Data API v3',apiKeyLabel:'API 키',apiKeyHelp:'channels.list(forHandle) 호출에 사용합니다. 로컬에만 저장되며 import/export에 포함되지 않습니다.',apiKeyPlaceholder:'API key를 입력하세요',apiKeySave:'API 키 저장',apiKeyTest:'API 키 테스트',apiKeyClear:'API 키 삭제',apiKeySaved:'API 키를 저장했습니다',apiKeyCleared:'API 키를 삭제했습니다',apiKeyStatusMissing:'API 키가 없습니다. Pair 생성/갱신 전에 저장해야 합니다.',apiKeyStatusSaved:(masked)=>`저장된 키: ${masked}`,apiKeyRequired:'Pair 생성/갱신에는 YouTube Data API v3 API 키가 필요합니다.',apiKeyTestIdle:'아직 API 키 테스트 기록이 없습니다.',apiKeyTestRunning:'API 키를 테스트하는 중입니다...',apiKeyTestLabel:'마지막 테스트',apiKeyTestResult:(category,message,statusText)=>`${category}${statusText ? `(${statusText})` : ''}: ${message}`,apiKeyTestOk:'정상',apiKeyTestInvalid:'잘못된 키',apiKeyTestQuota:'quota 초과',apiKeyTestForbidden:'권한 거부',apiKeyTestNetwork:'네트워크 실패',apiKeyTestUnknown:'알 수 없음',apiQuotaGuidanceTitle:'Quota 안내',apiQuotaGuidance:({count,reset})=>`반복 quota 실패 ${count}회. Google Cloud Console에서 YouTube Data API 할당량을 확인하고, 필요하면 quota reset 이후(${reset}) 다시 테스트하세요.`,pairCreate:'Pair 생성',pairUpdate:'Update Pair',pairWorking:'처리 중...',pairSummary:'Pair 상태',pairLastCheck:(s)=>s?`마지막 검사: ${s}`:'아직 검사 기록 없음',pairSummaryHandles:'핸들',pairSummaryPaired:'paired',pairSummaryNeeded:'pair 필요',pairSummaryStale:'stale',pairSummaryMismatch:'mismatch',pairSummaryUnverified:'unverified',badgeHandleOnly:'handle-only',badgePaired:'paired',badgeStale:'stale',badgeMismatch:'mismatch',badgeUnverified:'unverified',badgeUid:'uid-rule',badgeRegex:'regex',metaUid:(uid)=>`UID: ${uid}`,metaVerifiedAt:(s)=>`마지막 검증: ${s}`,metaResolvedUid:(uid)=>`최신 조회 UID: ${uid}`,metaSource:(source)=>`source: ${source}`,metaError:(msg)=>`오류: ${msg}`,pairBannerTitle:'UID pair 확인이 필요합니다.',pairBannerBody:(stale,mismatch)=>`stale ${stale}건, mismatch ${mismatch}건이 있어 Update Pair가 필요합니다.`,updateNow:'지금 업데이트',later:'나중에',pairResult:({created,refreshed,mismatches,failed,addedIds,skipped})=>`생성 ${created} / 갱신 ${refreshed} / mismatch ${mismatches} / 실패 ${failed} / UID 추가 ${addedIds} / skip ${skipped || 0}`,pairSkippedFresh:'아직 갱신 주기 내에 있어 API 조회를 건너뜀',pairUidReplaced:'mismatch 감지 후 UID 규칙을 교체했습니다',pairResultDetails:'최근 Pair 실행 결과',pairResultEmpty:'아직 Pair 실행 기록이 없습니다.',pairResultDialogTitle:'Pair 실행 상세',pairOutcomeCreated:'생성',pairOutcomeUpdated:'갱신',pairOutcomeMismatch:'mismatch',pairOutcomeFailed:'실패',pairOutcomeSkipped:'skip',pairResultFilterLabel:'결과 필터',pairResultFilterAll:'전체',pairResultSortLabel:'정렬',pairResultSortOriginal:'실행 순서',pairResultSortOutcome:'결과별',pairResultSortHandle:'handle',pairResultCopyFailed:'실패 handle 복사',pairResultExportFailed:'실패 handle 내보내기',pairResultFailedTitle:'실패 handle',pairResultFailedEmpty:'실패한 handle이 없습니다.',pairResultFailedCopied:(n)=>`${n}개 실패 handle을 복사했습니다`,pairResultSummary:({selected,visible,total})=>`선택 ${selected} / 표시 ${visible} / 전체 ${total}`,pairLookupFailed:'UID 조회 실패',pairLookupNoUid:'UID를 찾지 못했습니다.',handleCaseLabel:'Handle 대소문자 구분',handleCaseHelp:'꺼져 있으면 소문자 기준으로 비교하고, 켜져 있으면 정확한 대소문자로 비교합니다.',handleCaseLegacy:'기존 handle은 소문자로 저장됐을 수 있어 exact 보장은 새로 추가하거나 다시 저장한 항목부터 적용됩니다.',typeFilterLabel:'타입',typeAll:'all',typeHandle:'handle',typeId:'id',typeRegex:'regex',searchLabel:'검색',searchPlaceholder:'handle, id, regex 검색',searchNoMatches:'검색 결과가 없습니다.',tagFilterLabel:'태그',selectVisible:'현재 목록 전체 선택',selectedCount:(n)=>`선택 ${n}개`,bulkActionLabel:'일괄 작업',bulkDelete:'선택 삭제',bulkCreatePairs:'선택 handle Pair 생성',bulkUpdatePairs:'선택 handle Pair 갱신',execute:'실행',clearSelection:'선택 해제',bulkDeleteResult:(n)=>`${n}개 항목을 삭제했습니다`,bulkHandleRequired:'선택된 handle이 없습니다.',regexMatchedCount:(n)=>`매칭 handle ${n}개`,regexSelectMatches:'매칭 handle 선택',regexExpand:'펼치기',regexCollapse:'접기',regexShowAll:'전체 보기',regexShowMore:(shown,total)=>`${shown}/${total}개 표시, 더 보기`,regexShowLess:'접기',regexNoMatches:'현재 매칭되는 handle이 없습니다.',regexSelectedMatches:(n)=>`${n}개 handle을 선택했습니다`,noFilteredEntries:'현재 필터에 맞는 항목이 없습니다.',noEntries:'—'},en:{block:'Block',unblock:'Unblock',confirmBlock:'🛑 Block this channel?',confirmUnblock:'❌ Unblock this channel?',manageTitle:(n)=>`Blocked channels (${n})`,import:'Import',export:'Export',close:'Close',removed:(h)=>`Unblocked ${h}`,added:(h)=>`Blocked ${h}`,clear:'Reset',confirmClear:'Reset all blocked entries?',menuManage:'🔍 Manage block list',menuClear:'🗑️ Clear block list',versionTitle:'Script Info',versionValue:(version)=>`Version: ${version}`,syncToast:'Block list synced from another tab.',pairSyncToast:'UID pair settings synced from another tab.',exportHint:'Copy as JSON or line text.',json:'JSON',text:'Text',importTitle:'Import block list',importPlaceholder:'JSON or @handle/regex per line/comma',importBtn:'Import',importedCount:(n)=>`Imported ${n} entries`,menuHide:'Hide comments from this channel',menuUnhide:"Unhide this channel's comments",addRegex:'Add Regex',patternLabel:'Pattern',flagsLabel:'Flags',addBtn:'Add',invalidRegex:'Invalid regex',addedRegex:'Regex added',exists:'Already exists',testRegex:'Build/Test Regex',uidDetectionLabel:'UID Detection',uidDetectionHelp:'Handle blocking always stays on. This only adds UID matching.',apiKeyTitle:'YouTube Data API v3',apiKeyLabel:'API Key',apiKeyHelp:'Used for channels.list(forHandle). Stored locally only and excluded from import/export.',apiKeyPlaceholder:'Enter your API key',apiKeySave:'Save API Key',apiKeyTest:'Test API Key',apiKeyClear:'Clear API Key',apiKeySaved:'API key saved',apiKeyCleared:'API key cleared',apiKeyStatusMissing:'No API key saved. Save one before running pair actions.',apiKeyStatusSaved:(masked)=>`Saved key: ${masked}`,apiKeyRequired:'A YouTube Data API v3 API key is required for pair creation and updates.',apiKeyTestIdle:'No API key test has run yet.',apiKeyTestRunning:'Testing the API key...',apiKeyTestLabel:'Last test',apiKeyTestResult:(category,message,statusText)=>`${category}${statusText ? `(${statusText})` : ''}: ${message}`,apiKeyTestOk:'OK',apiKeyTestInvalid:'Invalid key',apiKeyTestQuota:'Quota exceeded',apiKeyTestForbidden:'Forbidden',apiKeyTestNetwork:'Network failure',apiKeyTestUnknown:'Unknown',apiQuotaGuidanceTitle:'Quota guidance',apiQuotaGuidance:({count,reset})=>`Repeated quota failures: ${count}. Check YouTube Data API quota in Google Cloud Console, then test again after the quota reset window (${reset}).`,pairCreate:'Create Pair',pairUpdate:'Update Pair',pairWorking:'Working...',pairSummary:'Pair Status',pairLastCheck:(s)=>s?`Last check: ${s}`:'No pair check has run yet.',pairSummaryHandles:'Handles',pairSummaryPaired:'Paired',pairSummaryNeeded:'Pair needed',pairSummaryStale:'Stale',pairSummaryMismatch:'Mismatch',pairSummaryUnverified:'Unverified',badgeHandleOnly:'handle-only',badgePaired:'paired',badgeStale:'stale',badgeMismatch:'mismatch',badgeUnverified:'unverified',badgeUid:'uid-rule',badgeRegex:'regex',metaUid:(uid)=>`UID: ${uid}`,metaVerifiedAt:(s)=>`Last verified: ${s}`,metaResolvedUid:(uid)=>`Latest UID: ${uid}`,metaSource:(source)=>`Source: ${source}`,metaError:(msg)=>`Error: ${msg}`,pairBannerTitle:'UID pair review required.',pairBannerBody:(stale,mismatch)=>`${stale} stale pair(s) and ${mismatch} mismatch pair(s) need an update.`,updateNow:'Update Now',later:'Later',pairResult:({created,refreshed,mismatches,failed,addedIds,skipped})=>`Created ${created} / Refreshed ${refreshed} / Mismatch ${mismatches} / Failed ${failed} / Added UID ${addedIds} / Skipped ${skipped || 0}`,pairSkippedFresh:'Skipped API lookup because the pair is still within the refresh interval',pairUidReplaced:'Replaced the UID rule after a mismatch',pairResultDetails:'Last Pair Run',pairResultEmpty:'No pair run has completed yet.',pairResultDialogTitle:'Pair Run Details',pairOutcomeCreated:'created',pairOutcomeUpdated:'updated',pairOutcomeMismatch:'mismatch',pairOutcomeFailed:'failed',pairOutcomeSkipped:'skipped',pairResultFilterLabel:'Result filter',pairResultFilterAll:'All',pairResultSortLabel:'Sort',pairResultSortOriginal:'Run order',pairResultSortOutcome:'Outcome',pairResultSortHandle:'Handle',pairResultCopyFailed:'Copy failed handles',pairResultExportFailed:'Export failed handles',pairResultFailedTitle:'Failed handles',pairResultFailedEmpty:'No failed handles.',pairResultFailedCopied:(n)=>`Copied ${n} failed handle(s)`,pairResultSummary:({selected,visible,total})=>`Selected ${selected} / Visible ${visible} / Total ${total}`,pairLookupFailed:'UID lookup failed',pairLookupNoUid:'Could not find a UID.',handleCaseLabel:'Handle Case Sensitive',handleCaseHelp:'Off compares normalized lowercase handles. On compares exact handle casing.',handleCaseLegacy:'Older handles may have been stored in lowercase, so exact matching is guaranteed only after re-saving or newly adding them.',typeFilterLabel:'Type',typeAll:'all',typeHandle:'handle',typeId:'id',typeRegex:'regex',searchLabel:'Search',searchPlaceholder:'Search handle, id, regex',searchNoMatches:'No search results.',tagFilterLabel:'Tags',selectVisible:'Select all visible',selectedCount:(n)=>`Selected ${n}`,bulkActionLabel:'Bulk Action',bulkDelete:'Delete selected',bulkCreatePairs:'Create pair for selected handles',bulkUpdatePairs:'Update pair for selected handles',execute:'Run',clearSelection:'Clear selection',bulkDeleteResult:(n)=>`Deleted ${n} item(s)`,bulkHandleRequired:'No selected handle entries.',regexMatchedCount:(n)=>`${n} matched handle(s)`,regexSelectMatches:'Select matching handles',regexExpand:'Expand',regexCollapse:'Collapse',regexShowAll:'Show all',regexShowMore:(shown,total)=>`Show more (${shown}/${total})`,regexShowLess:'Show less',regexNoMatches:'No blocked handles currently match this regex.',regexSelectedMatches:(n)=>`Selected ${n} matching handle(s)`,noFilteredEntries:'No entries match the current filters.',noEntries:'—'}};const getLang=()=>{try{return(GM_getValue('lang')||navigator.language||'ko').startsWith('ko')?'ko':'en';}catch{return'ko';}};const t=(key,...args)=>{const lang=getLang();const val=I18N[lang][key];return typeof val==='function'?val(...args):val;};const formatDateTime=(ts)=>{if(!Number.isFinite(ts)||ts<=0)return null;try{return new Date(ts).toLocaleString(getLang()==='ko'?'ko-KR':'en-US');}catch{return new Date(ts).toISOString();}};const makePlainTextNode=(value)=>{const div=document.createElement('div');div.textContent=String(value??'');return div;};const binaryHas=(arr,value)=>{let lo=0,hi=arr.length-1;while(lo<=hi){const mid=(lo+hi)>>1;if(arr[mid]===value)return true;if(arr[mid]<value)lo=mid+1;else hi=mid-1;}return false;};const intersectSortedPostings=(lists)=>{if(!lists.length)return[];const sortedLists=lists.filter(Boolean).sort((a,b)=>a.length-b.length);if(!sortedLists.length)return[];let result=sortedLists[0].slice();for(let i=1;i<sortedLists.length;i++){const next=sortedLists[i];result=result.filter(value=>binaryHas(next,value));if(!result.length)break;}return result;};const getSearchBigrams=(query)=>{const normalized=String(query||'').trim().toLowerCase();if(!normalized)return[];if(normalized.length<2)return[normalized];const grams=[];const seen=new Set();for(let i=0;i<normalized.length-1;i++){const gram=normalized.slice(i,i+2);if(seen.has(gram))continue;seen.add(gram);grams.push(gram);}return grams;};const buildManagerSearchIndex=(items)=>{const records=(items||[]).map((item,index)=>{const label=item?.type==='regex'?`/${item.value}/${item.flags || ''}`:String(item?.value||'');return{index,item,label,labelNorm:label.toLowerCase()};});const firstCharBuckets=new Map();const bigramIndex=new Map();for(const record of records){for(const ch of new Set(record.labelNorm.split(''))){if(!ch.trim())continue;if(!firstCharBuckets.has(ch))firstCharBuckets.set(ch,[]);firstCharBuckets.get(ch).push(record.index);}for(const gram of getSearchBigrams(record.labelNorm)){if(!bigramIndex.has(gram))bigramIndex.set(gram,[]);bigramIndex.get(gram).push(record.index);}}return{records,firstCharBuckets,bigramIndex};};const searchManagerIndex=(index,query)=>{const normalized=String(query||'').trim().toLowerCase();if(!normalized)return index.records.map(record=>record.item);let candidateIndexes=[];if(normalized.length===1){candidateIndexes=(index.firstCharBuckets.get(normalized)||[]).slice();}else{const grams=getSearchBigrams(normalized);const postings=grams.map(gram=>index.bigramIndex.get(gram)||[]);candidateIndexes=intersectSortedPostings(postings);}if(!candidateIndexes.length)return[];return candidateIndexes.map(idx=>index.records[idx]).filter(record=>record.labelNorm.includes(normalized)).map(record=>record.item);};const getApiTestCategoryLabel=(category)=>{switch(category){case'ok':return t('apiKeyTestOk');case'invalid':return t('apiKeyTestInvalid');case'quota':return t('apiKeyTestQuota');case'forbidden':return t('apiKeyTestForbidden');case'network':return t('apiKeyTestNetwork');default:return t('apiKeyTestUnknown');}};class AppSettingsStorage{constructor(){this.KEY='app_settings_v1';this._state=this._init();}_getGM(key,def){try{return GM_getValue(key,def);}catch{return def;}}_setGM(key,val){try{GM_setValue(key,val);}catch{}}_normalizeState(raw){const src=raw&&typeof raw==='object'?raw:{};return{version:1,handleCaseSensitive:!!src.handleCaseSensitive};}_init(){return this._normalizeState(this._getGM(this.KEY,null));}getState(){return{...this._state};}setAllLocal(state){this._state=this._normalizeState(state);return this.getState();}_saveState(nextState){const normalized=this._normalizeState(nextState);if(this._state.handleCaseSensitive===normalized.handleCaseSensitive){this._state=normalized;return this.getState();}this._state=normalized;this._setGM(this.KEY,this._state);return this.getState();}isHandleCaseSensitive(){return!!this._state.handleCaseSensitive;}setHandleCaseSensitive(enabled){return this._saveState({...this._state,handleCaseSensitive:!!enabled});}}class StorageV2{constructor(settings){this.settings=settings;this.KEY_LEGACY='blockedHandles';this.KEY_V1='blockedHandles_v1';this.KEY_V2='blocked_v2';this._items=this._init();}_getGM(key,def){try{return GM_getValue(key,def);}catch{return def;}}_setGM(key,val){try{GM_setValue(key,val);}catch{}}_loadLegacy(){const raw=this._getGM(this.KEY_LEGACY,[]);let handles=[];if(Array.isArray(raw))handles=raw;else if(typeof raw==='string')handles=raw.split(/\s*[,\n]\s*/);return handles.map(norm).filter(Boolean).map(h=>({type:'handle',value:h}));}_loadV1(){const v=this._getGM(this.KEY_V1,null);if(!v||typeof v!=='object'||v.version!==1||!Array.isArray(v.handles))return[];return v.handles.map(norm).filter(Boolean).map(h=>({type:'handle',value:h}));}_loadV2(){const v=this._getGM(this.KEY_V2,null);if(!v||typeof v!=='object'||v.version!==2||!Array.isArray(v.items))return[];return v.items.filter(it=>it&&typeof it.value==='string'&&['id','handle','regex'].includes(it.type));}_saveV2(items){const normed=[];const caseSensitive=this.settings?.isHandleCaseSensitive?.()||false;for(const it of items){if(!it||!it.value)continue;if(it.type==='handle'){const h=sanitizeHandle(it.value);if(!h)continue;normed.push({type:'handle',value:h});}else if(it.type==='id'){const id=String(it.value).trim();if(!isChannelId(id))continue;normed.push({type:'id',value:id});}else if(it.type==='regex'){const spec=validateRegexSpec(it.value,it.flags||'');if(spec)normed.push({type:'regex',value:spec.pattern,flags:spec.flags});}}const unique=[];const seen=new Set();for(const it of normed){const key=it.type==='handle'?`h:${getHandleCompareKey(it.value, caseSensitive)}`:it.type==='id'?`i:${it.value}`:`r:${it.value}/${it.flags || ''}`;if(seen.has(key))continue;seen.add(key);unique.push(it);}if(this._arraysEqual(this._items,unique)){this._items=unique;return unique;}this._setGM(this.KEY_V2,{version:2,updatedAt:Date.now(),items:unique});this._items=unique;return unique;}setAllLocal(items){this._items=items.slice();return this._items;}_arraysEqual(a,b){if(a===b)return true;if(!Array.isArray(a)||!Array.isArray(b))return false;if(a.length!==b.length)return false;for(let i=0;i<a.length;i++){const A=a[i],B=b[i];if(!A||!B||A.type!==B.type||A.value!==B.value||(A.flags||'')!==(B.flags||''))return false;}return true;}_init(){const merged=[...this._loadV2(),...this._loadV1(),...this._loadLegacy()];return this._saveV2(merged);}all(){return this._items.slice();}setAll(items){return this._saveV2(items);}addHandle(h){const v=sanitizeHandle(h);if(!v)return false;return!!this._saveV2([...this._items,{type:'handle',value:v}]);}addId(id){id=(id||'').trim();if(!isChannelId(id))return false;return!!this._saveV2([...this._items,{type:'id',value:id}]);}addRegex(pattern,flags=''){const spec=validateRegexSpec(pattern,flags);if(!spec)return false;const before=this._items.length;this._saveV2([...this._items,{type:'regex',value:spec.pattern,flags:spec.flags}]);return this._items.length>before;}remove(item){const key=getItemKey(item);return!!this._saveV2(this._items.filter(it=>{const k=getItemKey(it);return k!==key;}));}clear(){this._saveV2([]);}}class PairMetaStorage{constructor(settings){this.settings=settings;this.KEY='pair_meta_v1';this._state=this._init();}_getGM(key,def){try{return GM_getValue(key,def);}catch{return def;}}_setGM(key,val){try{GM_setValue(key,val);}catch{}}_defaultState(){return{version:1,enableUidDetection:false,lastPairCheckAt:null,pairNotificationDismissedAt:null,pairs:[]};}_normalizeStatus(pair,now=Date.now()){if(pair.status==='mismatch')return'mismatch';if(pair.status==='unverified')return'unverified';if(!pair.uid)return'unverified';if(!Number.isFinite(pair.verifiedAt)||pair.verifiedAt<=0)return'unverified';return now-pair.verifiedAt>=PAIR_STALE_MS?'stale':'verified';}_normalizePair(raw,now=Date.now()){const handle=sanitizeHandle(raw?.handle);if(!handle)return null;const uid=isChannelId(raw?.uid)?String(raw.uid).trim():'';const verifiedAt=Number.isFinite(raw?.verifiedAt)&&raw.verifiedAt>0?raw.verifiedAt:null;const source=typeof raw?.source==='string'&&raw.source.trim()?raw.source.trim():'unknown';const lastResolvedUid=isChannelId(raw?.lastResolvedUid)?String(raw.lastResolvedUid).trim():null;const lastError=typeof raw?.lastError==='string'&&raw.lastError.trim()?raw.lastError.trim():null;const normalized={handle,uid,verifiedAt,status:raw?.status||(uid?'verified':'unverified'),source,lastResolvedUid,lastError};normalized.status=this._normalizeStatus(normalized,now);return normalized;}_normalizeState(raw){const src=raw&&typeof raw==='object'?raw:{};const caseSensitive=this.settings?.isHandleCaseSensitive?.()||false;const next={version:1,enableUidDetection:!!src.enableUidDetection,lastPairCheckAt:Number.isFinite(src.lastPairCheckAt)?src.lastPairCheckAt:null,pairNotificationDismissedAt:Number.isFinite(src.pairNotificationDismissedAt)?src.pairNotificationDismissedAt:null,pairs:[]};const dedup=new Map();for(const pair of Array.isArray(src.pairs)?src.pairs:[]){const normalized=this._normalizePair(pair);if(normalized)dedup.set(getHandleCompareKey(normalized.handle,caseSensitive),normalized);}next.pairs=Array.from(dedup.values());return next;}_init(){return this._normalizeState(this._getGM(this.KEY,null));}_statesEqual(a,b){if(a===b)return true;if(!a||!b)return false;if(a.enableUidDetection!==b.enableUidDetection)return false;if((a.lastPairCheckAt||null)!==(b.lastPairCheckAt||null))return false;if((a.pairNotificationDismissedAt||null)!==(b.pairNotificationDismissedAt||null))return false;if(!Array.isArray(a.pairs)||!Array.isArray(b.pairs)||a.pairs.length!==b.pairs.length)return false;for(let i=0;i<a.pairs.length;i++){const A=a.pairs[i];const B=b.pairs[i];if(!A||!B)return false;if(A.handle!==B.handle||A.uid!==B.uid||(A.verifiedAt||null)!==(B.verifiedAt||null)||A.status!==B.status||A.source!==B.source||(A.lastResolvedUid||null)!==(B.lastResolvedUid||null)||(A.lastError||null)!==(B.lastError||null))return false;}return true;}_saveState(nextState){const normalized=this._normalizeState(nextState);if(this._statesEqual(this._state,normalized)){this._state=normalized;return this.getState();}this._state=normalized;this._setGM(this.KEY,this._state);return this.getState();}getState(){return{...this._state,pairs:this._state.pairs.map(pair=>({...pair}))};}setAllLocal(state){this._state=this._normalizeState(state);return this.getState();}refreshStatuses(){return this._saveState(this._state);}isUidDetectionEnabled(){return!!this._state.enableUidDetection;}setUidDetectionEnabled(enabled){return this._saveState({...this._state,enableUidDetection:!!enabled});}getLastPairCheckAt(){return this._state.lastPairCheckAt;}setLastPairCheckAt(ts){return this._saveState({...this._state,lastPairCheckAt:Number.isFinite(ts)?ts:null});}getNotificationDismissedAt(){return this._state.pairNotificationDismissedAt;}dismissNotification(ts=Date.now()){return this._saveState({...this._state,pairNotificationDismissedAt:Number.isFinite(ts)?ts:Date.now()});}allPairs(){return this._state.pairs.map(pair=>({...pair}));}getPair(handle){const normalized=getHandleCompareKey(handle,this.settings?.isHandleCaseSensitive?.()||false);if(!normalized)return null;return this._state.pairs.find(pair=>getHandleCompareKey(pair.handle,this.settings?.isHandleCaseSensitive?.()||false)===normalized)||null;}upsertPair(pair){const normalized=this._normalizePair(pair);if(!normalized)return this.getState();const compareKey=getHandleCompareKey(normalized.handle,this.settings?.isHandleCaseSensitive?.()||false);const nextPairs=this._state.pairs.filter(item=>getHandleCompareKey(item.handle,this.settings?.isHandleCaseSensitive?.()||false)!==compareKey);nextPairs.push(normalized);return this._saveState({...this._state,pairs:nextPairs});}removePair(handle){const normalized=getHandleCompareKey(handle,this.settings?.isHandleCaseSensitive?.()||false);if(!normalized)return this.getState();return this._saveState({...this._state,pairs:this._state.pairs.filter(pair=>getHandleCompareKey(pair.handle,this.settings?.isHandleCaseSensitive?.()||false)!==normalized)});}removePairs(handles){const keys=new Set((handles||[]).map(handle=>getHandleCompareKey(handle,this.settings?.isHandleCaseSensitive?.()||false)).filter(Boolean));if(!keys.size)return this.getState();return this._saveState({...this._state,pairs:this._state.pairs.filter(pair=>!keys.has(getHandleCompareKey(pair.handle,this.settings?.isHandleCaseSensitive?.()||false)))});}clearPairs(){return this._saveState({...this._state,pairs:[]});}}class ApiConfigStorage{constructor(){this.KEY='youtube_data_api_v3_config';this._state=this._init();}_getGM(key,def){try{return GM_getValue(key,def);}catch{return def;}}_setGM(key,val){try{GM_setValue(key,val);}catch{}}_defaultState(){return{version:2,apiKey:'',lastTestResult:null,quotaFailureCount:0,lastQuotaFailureAt:null};}_normalizeTestResult(raw){if(!raw||typeof raw!=='object')return null;const category=['ok','invalid','quota','forbidden','network','unknown'].includes(raw.category)?raw.category:'unknown';return{checkedAt:Number.isFinite(raw.checkedAt)?raw.checkedAt:Date.now(),ok:!!raw.ok,category,httpStatus:Number.isFinite(raw.httpStatus)?raw.httpStatus:null,message:typeof raw.message==='string'&&raw.message.trim()?raw.message.trim():(category==='ok'?'OK':'Unknown')};}_normalizeState(raw){const src=raw&&typeof raw==='object'?raw:{};return{version:2,apiKey:typeof src.apiKey==='string'?src.apiKey.trim():'',lastTestResult:this._normalizeTestResult(src.lastTestResult),quotaFailureCount:Number.isFinite(src.quotaFailureCount)&&src.quotaFailureCount>0?Math.floor(src.quotaFailureCount):0,lastQuotaFailureAt:Number.isFinite(src.lastQuotaFailureAt)?src.lastQuotaFailureAt:null};}_init(){return this._normalizeState(this._getGM(this.KEY,null));}getState(){return{...this._state};}setAllLocal(state){this._state=this._normalizeState(state);return this.getState();}_saveState(nextState){const normalized=this._normalizeState(nextState);const sameResult=JSON.stringify(this._state.lastTestResult)===JSON.stringify(normalized.lastTestResult);if(this._state.apiKey===normalized.apiKey&&sameResult&&this._state.quotaFailureCount===normalized.quotaFailureCount&&(this._state.lastQuotaFailureAt||null)===(normalized.lastQuotaFailureAt||null)){this._state=normalized;return this.getState();}this._state=normalized;this._setGM(this.KEY,this._state);return this.getState();}hasApiKey(){return!!this._state.apiKey;}getApiKey(){return this._state.apiKey;}getLastTestResult(){return this._state.lastTestResult?{...this._state.lastTestResult}:null;}getQuotaGuidance(){if(this._state.quotaFailureCount<2||!this._state.lastQuotaFailureAt)return null;return{count:this._state.quotaFailureCount,lastFailureAt:this._state.lastQuotaFailureAt,resetAt:this._state.lastQuotaFailureAt+24*60*60*1000};}setApiKey(apiKey){const nextKey=String(apiKey||'').trim();return this._saveState({...this._state,apiKey:nextKey,lastTestResult:this._state.apiKey===nextKey?this._state.lastTestResult:null,quotaFailureCount:this._state.apiKey===nextKey?this._state.quotaFailureCount:0,lastQuotaFailureAt:this._state.apiKey===nextKey?this._state.lastQuotaFailureAt:null});}clearApiKey(){return this._saveState({...this._state,apiKey:'',lastTestResult:null,quotaFailureCount:0,lastQuotaFailureAt:null});}setLastTestResult(result){const normalized=this._normalizeTestResult(result);const quotaFailureCount=normalized?.category==='quota'?this._state.quotaFailureCount+1:(normalized?.ok?0:this._state.quotaFailureCount);return this._saveState({...this._state,lastTestResult:normalized,quotaFailureCount,lastQuotaFailureAt:normalized?.category==='quota'?normalized.checkedAt:this._state.lastQuotaFailureAt});}clearLastTestResult(){return this._saveState({...this._state,lastTestResult:null,quotaFailureCount:0,lastQuotaFailureAt:null});}getMaskedApiKey(){const key=this.getApiKey();if(!key)return'';if(key.length<=8)return'•'.repeat(key.length);return`${key.slice(0, 4)}...${key.slice(-4)}`;}}class PairService{constructor(storage,pairStore,apiConfig,settings){this.storage=storage;this.pairStore=pairStore;this.apiConfig=apiConfig;this.settings=settings;this._busy=false;}getBlockedHandles(){return this.storage.all().filter(item=>item.type==='handle').map(item=>item.value);}getBlockedIdSet(items=this.storage.all()){return new Set((items||[]).filter(item=>item.type==='id').map(item=>item.value));}hasBlockedId(uid,blockedIds=null){return blockedIds?blockedIds.has(uid):this.storage.all().some(item=>item.type==='id'&&item.value===uid);}getHandleStatus(handle,blockedIds=null){const pair=this.pairStore.getPair(handle);if(!pair)return{code:'handle-only',pair:null};if(pair.status==='mismatch')return{code:'mismatch',pair};if(pair.status==='unverified'||!pair.uid)return{code:'unverified',pair};if(!this.hasBlockedId(pair.uid,blockedIds))return{code:'handle-only',pair};if(pair.status==='stale')return{code:'stale',pair};return{code:'paired',pair};}getSummary(){this.pairStore.refreshStatuses();const allItems=this.storage.all();const blockedIds=this.getBlockedIdSet(allItems);const summary={handles:0,paired:0,handleOnly:0,stale:0,mismatch:0,unverified:0,pairNeeded:0};for(const handle of allItems.filter(item=>item.type==='handle').map(item=>item.value)){summary.handles+=1;const status=this.getHandleStatus(handle,blockedIds).code;if(status==='paired')summary.paired+=1;else if(status==='stale')summary.stale+=1;else if(status==='mismatch')summary.mismatch+=1;else if(status==='unverified')summary.unverified+=1;else summary.handleOnly+=1;}summary.pairNeeded=summary.handleOnly+summary.unverified;return summary;}shouldNotify(){if(!this.pairStore.isUidDetectionEnabled())return false;const summary=this.getSummary();if(!summary.stale&&!summary.mismatch)return false;const dismissedAt=this.pairStore.getNotificationDismissedAt();return!dismissedAt||(Date.now()-dismissedAt)>=PAIR_NOTICE_COOLDOWN_MS;}dismissNotification(){this.pairStore.dismissNotification();}removeHandleArtifacts(handle){const pair=this.pairStore.getPair(handle);if(pair?.uid)this.storage.remove({type:'id',value:pair.uid});this.pairStore.removePair(handle);}collectHandleArtifactIds(handles){const ids=new Set();for(const handle of handles||[]){const pair=this.pairStore.getPair(handle);if(pair?.uid)ids.add(pair.uid);}return ids;}removeHandlePairs(handles){this.pairStore.removePairs(handles||[]);}clearPairArtifacts(){this.pairStore.clearPairs();}async createMissingPairs(){const handles=this.getBlockedHandles().filter(handle=>{const code=this.getHandleStatus(handle).code;return code==='handle-only'||code==='unverified';});return this._processHandles(handles);}async createPairsForHandles(handles){const filtered=(handles||[]).filter(handle=>{const code=this.getHandleStatus(handle).code;return code==='handle-only'||code==='unverified';});return this._processHandles(filtered);}_shouldRefreshHandle(handle,{includeMissing=true}={}){const status=this.getHandleStatus(handle).code;if(status==='paired')return false;if(status==='handle-only')return includeMissing;return status==='stale'||status==='mismatch'||status==='unverified';}async updatePairs({includeMissing=true}={}){const handles=[];const skipped=[];for(const handle of this.getBlockedHandles()){const hasPair=!!this.pairStore.getPair(handle);if(!includeMissing&&!hasPair)continue;if(this._shouldRefreshHandle(handle,{includeMissing}))handles.push(handle);else skipped.push({handle,outcome:'skipped',message:t('pairSkippedFresh')});}if(!handles.length){return{created:0,refreshed:0,mismatches:0,failed:0,addedIds:0,skipped:skipped.length,items:skipped};}const stats=await this._processHandles(handles);stats.skipped+=skipped.length;stats.items.push(...skipped);return stats;}async updatePairsForHandles(handles){return this._processHandles(handles||[]);}async _processHandles(handles){if(this._busy)return{created:0,refreshed:0,mismatches:0,failed:0,addedIds:0,skipped:handles.length,items:(handles||[]).map(handle=>({handle:sanitizeHandle(handle)||String(handle||''),outcome:'skipped',message:'busy'}))};this._busy=true;const stats={created:0,refreshed:0,mismatches:0,failed:0,addedIds:0,skipped:0,items:[]};const uniqueHandles=[];const seen=new Set();for(const handle of handles||[]){const key=getHandleCompareKey(handle,this.settings?.isHandleCaseSensitive?.()||false);const value=sanitizeHandle(handle);if(!key||!value||seen.has(key))continue;seen.add(key);uniqueHandles.push(value);}try{for(const handle of uniqueHandles){const existing=this.pairStore.getPair(handle);try{const resolved=await this.resolveHandle(handle);if(existing?.uid&&existing.uid!==resolved.uid){this.pairStore.upsertPair({...existing,handle,uid:resolved.uid,verifiedAt:Date.now(),status:'verified',lastResolvedUid:resolved.uid,lastError:null,source:resolved.source||existing.source||'youtube-data-api-v3'});if(this.hasBlockedId(existing.uid))this.storage.remove({type:'id',value:existing.uid});if(!this.hasBlockedId(resolved.uid)&&this.storage.addId(resolved.uid))stats.addedIds+=1;stats.mismatches+=1;stats.items.push({handle,outcome:'mismatch',uid:existing.uid,resolvedUid:resolved.uid,message:t('pairUidReplaced')});continue;}this.pairStore.upsertPair({handle,uid:resolved.uid,verifiedAt:Date.now(),status:'verified',source:resolved.source,lastResolvedUid:resolved.uid,lastError:null});if(!this.hasBlockedId(resolved.uid)&&this.storage.addId(resolved.uid)){stats.addedIds+=1;}if(existing?.uid){stats.refreshed+=1;stats.items.push({handle,outcome:'updated',uid:resolved.uid});}else{stats.created+=1;stats.items.push({handle,outcome:'created',uid:resolved.uid});}}catch(error){const message=error instanceof Error?error.message:String(error);const fallbackStatus=existing?.uid?(existing.status==='mismatch'?'mismatch':(existing.verifiedAt&&(Date.now()-existing.verifiedAt)>=PAIR_STALE_MS?'stale':'unverified')):'unverified';this.pairStore.upsertPair({...existing,handle,uid:existing?.uid||'',verifiedAt:existing?.verifiedAt||null,status:fallbackStatus,source:existing?.source||'youtube-data-api-v3',lastResolvedUid:existing?.lastResolvedUid||null,lastError:message});stats.failed+=1;stats.items.push({handle,outcome:'failed',uid:existing?.uid||undefined,resolvedUid:existing?.lastResolvedUid||undefined,message});}}}finally{this._busy=false;this.pairStore.setLastPairCheckAt(Date.now());this.pairStore.refreshStatuses();}return stats;}async testApiKey(){const apiKey=this.apiConfig.getApiKey();if(!apiKey){return{checkedAt:Date.now(),ok:false,category:'invalid',httpStatus:null,message:t('apiKeyRequired')};}try{const url=new URL('https://www.googleapis.com/youtube/v3/channels');url.searchParams.set('part','id');url.searchParams.set('id','UC_x5XG1OV2P6uZZ5FSM9Ttw');url.searchParams.set('key',apiKey);url.searchParams.set('hl',getLang()==='ko'?'ko':'en');const response=await fetch(url.toString(),{cache:'no-store'});let payload=null;try{payload=await response.json();}catch{}const reason=payload?.error?.errors?.[0]?.reason||'';const message=payload?.error?.message||(response.ok?'OK':`${response.status}`);if(response.ok){return{checkedAt:Date.now(),ok:true,category:'ok',httpStatus:response.status,message:'OK'};}const category=reason.includes('quota')||reason.includes('dailyLimit')?'quota':response.status===400?'invalid':response.status===403?'forbidden':'unknown';return{checkedAt:Date.now(),ok:false,category,httpStatus:response.status,message};}catch(error){return{checkedAt:Date.now(),ok:false,category:'network',httpStatus:null,message:error instanceof Error?error.message:String(error)};}}async resolveHandle(handle){const apiKey=this.apiConfig.getApiKey();if(!apiKey)throw new Error(t('apiKeyRequired'));const url=new URL('https://www.googleapis.com/youtube/v3/channels');url.searchParams.set('part','id');url.searchParams.set('forHandle',handle);url.searchParams.set('key',apiKey);url.searchParams.set('hl',getLang()==='ko'?'ko':'en');const response=await fetch(url.toString(),{cache:'no-store'});let payload=null;try{payload=await response.json();}catch{}if(!response.ok){const message=payload?.error?.message||`${t('pairLookupFailed')} (${response.status})`;throw new Error(message);}const uid=payload?.items?.[0]?.id;if(!isChannelId(uid))throw new Error(t('pairLookupNoUid'));return{uid,source:'youtube-data-api-v3'};}}class Toast{static show(msg,ms=2000){const el=Object.assign(document.createElement('div'),{className:'tm-toast'});el.setAttribute('aria-live','polite');el.textContent=msg;document.body.appendChild(el);requestAnimationFrame(()=>el.style.opacity='1');setTimeout(()=>{el.style.opacity='0';setTimeout(()=>el.remove(),200);},ms);}}class Dialog{static _instances=new Set();static show({title='',body=null,buttons=[],onBeforeClose=null,onRefresh=null}){return new Promise(resolve=>{const backdrop=Object.assign(document.createElement('div'),{className:'tm-backdrop'});const dialog=Object.assign(document.createElement('div'),{className:'tm-dialog'});dialog.setAttribute('role','dialog');dialog.setAttribute('aria-modal','true');const header=document.createElement('header');header.textContent=title;dialog.setAttribute('aria-label',title);const content=Object.assign(document.createElement('div'),{className:'tm-content'});if(body instanceof Node)content.appendChild(body);else if(typeof body==='string'){content.appendChild(makePlainTextNode(body));}const footer=document.createElement('footer');const renderedButtons=[];const refreshContext={dialog,header,content,footer,buttons:renderedButtons,setTitle:(nextTitle)=>{header.textContent=nextTitle;dialog.setAttribute('aria-label',nextTitle);},setBody:(nextBody)=>{content.replaceChildren();if(nextBody instanceof Node)content.appendChild(nextBody);else content.appendChild(makePlainTextNode(nextBody));}};const close=(val)=>{try{if(onBeforeClose)val=onBeforeClose(val,dialog);}catch{}backdrop.remove();document.removeEventListener('keydown',onKey);Dialog._instances.delete(instance);resolve(val);};buttons.forEach(btn=>{const b=Object.assign(document.createElement('button'),{textContent:btn.label,className:btn.primary?'primary':'secondary'});b.addEventListener('click',()=>close(btn.value));renderedButtons.push(b);footer.appendChild(b);});dialog.append(header,content,footer);backdrop.appendChild(dialog);document.body.appendChild(backdrop);const onKey=(e)=>{if(e.key==='Escape'){e.preventDefault();close(false);}else if(e.key==='Enter'){const target=e.target;if(target&&(['TEXTAREA','INPUT','SELECT'].includes(target.tagName)||target.isContentEditable))return;const primary=buttons.find(b=>b.primary)?.value??true;e.preventDefault();close(primary);}else if(e.key==='Tab'){const focusables=dialog.querySelectorAll('button, textarea, [href], input, select, [tabindex]:not([tabindex="-1"])');if(!focusables.length)return;const first=focusables[0],last=focusables[focusables.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}};document.addEventListener('keydown',onKey);backdrop.addEventListener('click',e=>{if(e.target===backdrop)close(false);});const instance={refresh:()=>onRefresh?.(refreshContext)};Dialog._instances.add(instance);if(onRefresh)onRefresh(refreshContext);dialog.querySelector('button')?.focus();});}static refreshAll(){for(const instance of Array.from(Dialog._instances)){try{instance.refresh?.();}catch{}}}}class Extractor{static getHandle(root){if(!root)return null;const span=root.querySelector('#author-text > span, #author-handle');const t=span?.textContent?.trim();if(t?.startsWith('@'))return sanitizeHandle(t);const a=root.querySelector('a[href^="/@"]');if(a?.getAttribute){const href=a.getAttribute('href')||'';const m=/^\/@([^/?#]+)/.exec(href);if(m)return sanitizeHandle('@'+decodeMaybe(m[1]));}return null;}static getChannelId(root){if(!root)return null;const a=root.querySelector('a[href*="/channel/UC"]');const href=a?.getAttribute?.('href')||'';const m=/\/channel\/(UC[0-9A-Za-z_-]+)/.exec(href);if(m)return m[1];return null;}static getCommentRoot(node){return node?.closest?.('ytd-comment-thread-renderer, ytd-comment-renderer, ytd-comment-view-model')||null;}}class CommentHider{constructor(storage,pairStore,settings){this.storage=storage;this.pairStore=pairStore;this.settings=settings;this._idSet=new Set();this._handleSet=new Set();this._regexes=[];this._metaCache=new WeakMap();this._observed=new Set();this._pending=false;this._pendingRoot=null;this._pendingFrame=null;this._io=null;this._metrics={mutationBatches:0,fullRefreshes:0,incrementalRefreshes:0,scannedNodes:0,lastDurationMs:0,totalDurationMs:0};this.rebuildLookup();try{window.__ytCommentBlockerPerf=this._metrics;}catch{}}rebuildLookup(){this._idSet.clear();this._handleSet.clear();this._regexes=[];const useUidDetection=this.pairStore.isUidDetectionEnabled();const caseSensitive=this.settings?.isHandleCaseSensitive?.()||false;for(const it of this.storage.all()){if(it.type==='id'){if(useUidDetection)this._idSet.add(it.value);}else if(it.type==='handle'){const key=getHandleCompareKey(it.value,caseSensitive);if(key)this._handleSet.add(key);}else if(it.type==='regex'){const spec=validateRegexSpec(it.value,it.flags||'');if(spec)this._regexes.push(new RegExp(spec.pattern,spec.flags));}}}_getDefaultRoot(){return document.querySelector(COMMENTS_HOST_SELECTOR);}_collectCommentNodes(root){if(!root)return[];if(root.matches?.(COMMENT_SELECTOR))return[root];if(!root.querySelectorAll)return[];return Array.from(root.querySelectorAll(COMMENT_SELECTOR));}_mergeRoots(a,b){if(!a)return b;if(!b||a===b)return a;if(a.contains?.(b))return a;if(b.contains?.(a))return b;return this._getDefaultRoot()||b;}_getMeta(node){const cached=this._metaCache.get(node);if(cached)return cached;const meta={id:Extractor.getChannelId(node),handle:Extractor.getHandle(node)};this._metaCache.set(node,meta);return meta;}invalidateNode(node){if(!node)return;this._metaCache.delete(node);}_matches(node){const meta=this._getMeta(node);if(meta.id&&this._idSet.has(meta.id))return true;const h=meta.handle;const handleKey=getHandleCompareKey(h,this.settings?.isHandleCaseSensitive?.()||false);if(handleKey&&this._handleSet.has(handleKey))return true;if(h){for(const rx of this._regexes){if(safeRegexTest(rx,h))return true;}}return false;}applyHide(node){if(!node)return;node.classList.toggle('tm-hidden',this._matches(node));}_connectIO(){if(this._io)return this._io;this._io=new IntersectionObserver((entries)=>{for(const e of entries)if(e.isIntersecting)this.applyHide(e.target);},{root:null,rootMargin:'0px',threshold:0});return this._io;}resetObservation(){if(this._io)this._io.disconnect();this._io=null;this._observed=new Set();}resetTransientState(){if(this._pendingFrame!==null){cancelAnimationFrame(this._pendingFrame);this._pendingFrame=null;}this.resetObservation();this._metaCache=new WeakMap();this._pending=false;this._pendingRoot=null;}_observeNode(node){if(!node||this._observed.has(node))return;this._observed.add(node);this._connectIO().observe(node);}unobserveNodes(nodes){if(!this._io)return;for(const node of nodes||[]){if(!this._observed.delete(node))continue;this._io.unobserve(node);}}_recordRefresh(kind,count,startedAt){this._metrics[kind]+=1;this._metrics.scannedNodes+=count;const duration=Math.round((performance.now()-startedAt)*100)/100;this._metrics.lastDurationMs=duration;this._metrics.totalDurationMs=Math.round((this._metrics.totalDurationMs+duration)*100)/100;}noteMutationBatch(){this._metrics.mutationBatches+=1;}refreshNodes(nodes,{invalidate=true}={}){const unique=new Set();for(const node of nodes||[]){if(node?.isConnected)unique.add(node);}if(!unique.size)return;const startedAt=performance.now();for(const node of unique){if(invalidate)this.invalidateNode(node);this.applyHide(node);this._observeNode(node);}this._recordRefresh('incrementalRefreshes',unique.size,startedAt);}doRefresh(root){const scope=root||this._getDefaultRoot();if(!scope)return;const nodes=this._collectCommentNodes(scope);if(!nodes.length)return;const startedAt=performance.now();for(const node of nodes){this.applyHide(node);this._observeNode(node);}this._recordRefresh('fullRefreshes',nodes.length,startedAt);}refreshScheduled(root){const scope=root||this._getDefaultRoot();if(!scope)return;this._pendingRoot=this._mergeRoots(this._pendingRoot,scope);if(this._pending)return;this._pending=true;this._pendingFrame=requestAnimationFrame(()=>{const nextRoot=this._pendingRoot||this._getDefaultRoot();this._pending=false;this._pendingFrame=null;this._pendingRoot=null;if(nextRoot)this.doRefresh(nextRoot);});}}class MenuEnhancer{constructor(app){this.app=app;this.storage=app.storage;this.lastHandle=null;this._popupObserver=null;this._popupTimer=null;document.body.addEventListener('click',e=>{const btn=e.target.closest?.('ytd-menu-renderer yt-icon-button#button, ytd-menu-renderer #button');if(!btn)return;const comment=Extractor.getCommentRoot(btn);this.lastHandle=Extractor.getHandle(comment);this._watchNextPopup();},true);window.addEventListener('yt-navigate-finish',()=>this._disconnectPopupObserver(),true);window.addEventListener('popstate',()=>this._disconnectPopupObserver(),true);}_disconnectPopupObserver(){if(this._popupObserver)this._popupObserver.disconnect();this._popupObserver=null;if(this._popupTimer)clearTimeout(this._popupTimer);this._popupTimer=null;}_watchNextPopup(){this._disconnectPopupObserver();this._popupObserver=new MutationObserver(muts=>{for(const m of muts){m.addedNodes?.forEach(n=>{if(n.nodeType!==1)return;if(!n.matches?.('ytd-menu-popup-renderer')||n.hasAttribute('tm-enhanced'))return;const listbox=n.querySelector?.('tp-yt-paper-listbox[role="menu"]');if(listbox&&this.lastHandle)this._addItem(listbox,this.lastHandle);n.setAttribute('tm-enhanced','');this._disconnectPopupObserver();});}});this._popupObserver.observe(document.body,{childList:true,subtree:true});this._popupTimer=setTimeout(()=>this._disconnectPopupObserver(),2000);}_addItem(menu,handle){const isBlocked=this.app.hasHandleRule(handle);const item=Object.assign(document.createElement('tp-yt-paper-item'),{className:'style-scope ytd-menu-service-item-renderer tm-hide-channel',role:'menuitem'});item.textContent=isBlocked?t('menuUnhide'):t('menuHide');item.addEventListener('click',async ev=>{ev.stopPropagation();const ok=await Dialog.show({title:isBlocked?t('unblock'):t('block'),body:(()=>{const div=document.createElement('div');const p=document.createElement('p');p.textContent=isBlocked?t('confirmUnblock'):t('confirmBlock');const b=document.createElement('b');b.textContent=handle;div.append(p,b);return div;})(),buttons:[{label:t('close'),value:false},{label:isBlocked?t('unblock'):t('block'),value:true,primary:true}],onRefresh:(ctx)=>{ctx.setTitle(isBlocked?t('unblock'):t('block'));ctx.content.querySelector('p').textContent=isBlocked?t('confirmUnblock'):t('confirmBlock');ctx.buttons[0].textContent=t('close');ctx.buttons[1].textContent=isBlocked?t('unblock'):t('block');}});if(!ok)return;if(isBlocked){this.app.removeHandleRule(handle);Toast.show(t('removed',handle));}else{this.app.addHandleRule(handle);Toast.show(t('added',handle));}document.body.click();});menu.appendChild(item);}}class BlockListManager{constructor(app){this.app=app;}_makeBadge(code){const badge=document.createElement('span');badge.className=`tm-badge ${code}`;const key=code==='uid'?'badgeUid':code==='regex'?'badgeRegex':code==='paired'?'badgePaired':code==='stale'?'badgeStale':code==='mismatch'?'badgeMismatch':code==='unverified'?'badgeUnverified':'badgeHandleOnly';badge.textContent=t(key);return badge;}_createMetaLine(text){const div=document.createElement('div');div.textContent=text;return div;}_getPairOutcomeLabel(code){if(code==='created')return t('pairOutcomeCreated');if(code==='updated')return t('pairOutcomeUpdated');if(code==='mismatch')return t('pairOutcomeMismatch');if(code==='failed')return t('pairOutcomeFailed');return t('pairOutcomeSkipped');}_getRegexMatches(regexItem,items){if(!regexItem||regexItem.type!=='regex')return[];const handles=(items||[]).filter(item=>item.type==='handle');const spec=validateRegexSpec(regexItem.value,regexItem.flags||'');if(!spec)return[];const rx=new RegExp(spec.pattern,spec.flags);return handles.filter(item=>safeRegexTest(rx,item.value));}_getPairResultItems(stats,options={}){const items=Array.isArray(stats?.items)?stats.items.slice():[];const filter=options.filter||'all';const sort=options.sort||'original';const filtered=filter==='all'?items:items.filter(item=>item.outcome===filter);if(sort==='handle'){filtered.sort((a,b)=>String(a.handle||'').localeCompare(String(b.handle||'')));}else if(sort==='outcome'){const order=['failed','mismatch','created','updated','skipped'];filtered.sort((a,b)=>{const ai=order.indexOf(a.outcome);const bi=order.indexOf(b.outcome);return(ai<0?order.length:ai)-(bi<0?order.length:bi)||String(a.handle||'').localeCompare(String(b.handle||''));});}return filtered;}_getFailedPairHandles(stats){return(stats?.items||[]).filter(item=>item?.outcome==='failed'&&item.handle).map(item=>item.handle);}_copyText(text){const value=String(text||'');try{if(navigator.clipboard?.writeText)return navigator.clipboard.writeText(value);}catch{}const ta=document.createElement('textarea');ta.value=value;ta.setAttribute('readonly','readonly');ta.style.position='fixed';ta.style.left='-9999px';document.body.appendChild(ta);try{ta.focus();if(typeof ta.select==='function')ta.select();if(typeof document.execCommand==='function')document.execCommand('copy');}catch{}ta.remove();return Promise.resolve();}_showFailedPairExport(handles){const body=document.createElement('div');const p=document.createElement('p');p.textContent=handles.length?t('exportHint'):t('pairResultFailedEmpty');const ta=document.createElement('textarea');ta.readOnly=true;ta.value=handles.join('\n');body.append(p,ta);Dialog.show({title:t('pairResultFailedTitle'),body,buttons:[{label:t('close'),value:false,primary:true}],onRefresh:(ctx)=>{ctx.setTitle(t('pairResultFailedTitle'));p.textContent=handles.length?t('exportHint'):t('pairResultFailedEmpty');ctx.buttons[0].textContent=t('close');}});}_renderPairResultList(container,stats){const previousOpen=container.querySelector('details')?.open;const state=container.__pairResultState||{filter:'all',sort:'original'};container.__pairResultState=state;container.replaceChildren();if(!stats?.items?.length){container.textContent=t('pairResultEmpty');container.className='tm-inline-note';return;}container.className='tm-result-panel';const details=document.createElement('details');details.open=typeof previousOpen==='boolean'?previousOpen:true;const summary=document.createElement('summary');summary.textContent=t('pairResultDetails');const controls=document.createElement('div');controls.className='tm-inline-actions';const filterLabel=document.createElement('label');filterLabel.textContent=t('pairResultFilterLabel');const filterSelect=document.createElement('select');const outcomes=['all','created','updated','mismatch','failed','skipped'];for(const outcome of outcomes){const option=document.createElement('option');option.value=outcome;option.textContent=outcome==='all'?t('pairResultFilterAll'):this._getPairOutcomeLabel(outcome);filterSelect.appendChild(option);}filterSelect.value=state.filter;const sortLabel=document.createElement('label');sortLabel.textContent=t('pairResultSortLabel');const sortSelect=document.createElement('select');[['original',t('pairResultSortOriginal')],['outcome',t('pairResultSortOutcome')],['handle',t('pairResultSortHandle')]].forEach(([value,label])=>{const option=document.createElement('option');option.value=value;option.textContent=label;sortSelect.appendChild(option);});sortSelect.value=state.sort;const failedHandles=this._getFailedPairHandles(stats);const copyFailedBtn=Object.assign(document.createElement('button'),{textContent:t('pairResultCopyFailed'),disabled:!failedHandles.length});const exportFailedBtn=Object.assign(document.createElement('button'),{textContent:t('pairResultExportFailed'),disabled:!failedHandles.length});filterSelect.addEventListener('change',()=>{state.filter=filterSelect.value||'all';this._renderPairResultList(container,stats);});sortSelect.addEventListener('change',()=>{state.sort=sortSelect.value||'original';this._renderPairResultList(container,stats);});copyFailedBtn.addEventListener('click',()=>{this._copyText(failedHandles.join('\n'));Toast.show(t('pairResultFailedCopied',failedHandles.length));});exportFailedBtn.addEventListener('click',()=>this._showFailedPairExport(failedHandles));controls.append(filterLabel,filterSelect,sortLabel,sortSelect,copyFailedBtn,exportFailedBtn);const list=document.createElement('ul');list.className='tm-result-list';for(const item of this._getPairResultItems(stats,state)){const li=document.createElement('li');const title=document.createElement('div');title.innerHTML='';const outcome=document.createElement('span');outcome.className='tm-result-outcome';outcome.textContent=this._getPairOutcomeLabel(item.outcome);const handle=document.createElement('span');handle.textContent=` ${item.handle}`;title.append(outcome,handle);li.appendChild(title);if(item.uid)li.appendChild(this._createMetaLine(t('metaUid',item.uid)));if(item.resolvedUid&&item.resolvedUid!==item.uid){li.appendChild(this._createMetaLine(t('metaResolvedUid',item.resolvedUid)));}if(item.message)li.appendChild(this._createMetaLine(item.message));list.appendChild(li);}details.append(summary,controls,list);container.appendChild(details);}_renderApiTestStatus(container,result,isRunning){container.replaceChildren();container.className='tm-inline-note';if(isRunning){container.textContent=t('apiKeyTestRunning');return;}if(!result){container.textContent=t('apiKeyTestIdle');return;}const category=getApiTestCategoryLabel(result.category);const statusText=result.httpStatus?String(result.httpStatus):'';const line=document.createElement('div');line.textContent=`${t('apiKeyTestLabel')}: ${t('apiKeyTestResult', category, result.message, statusText)} (${formatDateTime(result.checkedAt) || ''})`;container.appendChild(line);const guidance=this.app?.apiConfig?.getQuotaGuidance?.();if(guidance){const quota=document.createElement('div');quota.className='tm-inline-note';const strong=document.createElement('strong');strong.textContent=t('apiQuotaGuidanceTitle');const body=document.createElement('div');body.textContent=t('apiQuotaGuidance',{count:guidance.count,reset:formatDateTime(guidance.resetAt)||''});quota.append(strong,body);container.appendChild(quota);}}_showPairResultDialog(stats){const body=document.createElement('div');this._renderPairResultList(body,stats);Dialog.show({title:t('pairResultDialogTitle'),body,buttons:[{label:t('close'),value:false,primary:true}],onRefresh:(ctx)=>{ctx.setTitle(t('pairResultDialogTitle'));ctx.buttons[0].textContent=t('close');this._renderPairResultList(body,stats);}});}openList(){this.app.pairStore.refreshStatuses();const wrap=document.createElement('div');const selection=new Set();const tagFilters=new Set();const expandedRegexKeys=new Set();const showAllRegexKeys=new Map();let busy=false;let apiTestBusy=false;let searchQuery='';const versionSection=document.createElement('section');versionSection.className='tm-section';const versionTitle=document.createElement('h3');const versionText=document.createElement('div');versionText.className='tm-muted';versionSection.append(versionTitle,versionText);const settingsSection=document.createElement('section');settingsSection.className='tm-section';const settingsTitle=document.createElement('h3');const settingsRow=document.createElement('div');settingsRow.className='tm-toggle-row';const settingsBox=document.createElement('div');const caseLabel=document.createElement('label');const caseToggle=document.createElement('input');caseToggle.type='checkbox';caseToggle.checked=this.app.settings.isHandleCaseSensitive();const caseText=document.createElement('span');caseText.textContent=t('handleCaseLabel');caseLabel.append(caseToggle,caseText);const caseHelp=document.createElement('p');caseHelp.textContent=t('handleCaseHelp');const caseLegacy=document.createElement('p');caseLegacy.textContent=t('handleCaseLegacy');settingsBox.append(caseLabel,caseHelp,caseLegacy);settingsRow.append(settingsBox);settingsSection.append(settingsTitle,settingsRow);const apiSection=document.createElement('section');apiSection.className='tm-section';const apiTitle=document.createElement('h3');apiTitle.textContent=t('apiKeyTitle');const apiRow=document.createElement('div');apiRow.className='tm-toggle-row';const apiBox=document.createElement('div');const apiLabel=document.createElement('label');const apiInput=document.createElement('input');apiInput.type='password';apiInput.style.minWidth='280px';apiInput.style.width='min(420px, 100%)';apiInput.value=this.app.apiConfig.getApiKey();const apiHelp=document.createElement('p');const apiStatus=document.createElement('div');apiStatus.className='tm-muted';const apiTestStatus=document.createElement('div');apiTestStatus.className='tm-inline-note';apiBox.append(apiLabel,apiInput,apiHelp,apiStatus);apiBox.appendChild(apiTestStatus);const apiActions=document.createElement('div');apiActions.className='tm-inline-actions';const saveApiBtn=Object.assign(document.createElement('button'),{className:'primary'});const testApiBtn=Object.assign(document.createElement('button'),{className:'secondary'});const clearApiBtn=Object.assign(document.createElement('button'),{className:'secondary'});apiActions.append(saveApiBtn,testApiBtn,clearApiBtn);apiRow.append(apiBox,apiActions);apiSection.append(apiTitle,apiRow);const pairSection=document.createElement('section');pairSection.className='tm-section';const pairTitle=document.createElement('h3');const toggleRow=document.createElement('div');toggleRow.className='tm-toggle-row';const toggleBox=document.createElement('div');const toggleLabel=document.createElement('label');const toggle=document.createElement('input');toggle.type='checkbox';toggle.checked=this.app.pairStore.isUidDetectionEnabled();const toggleText=document.createElement('span');toggleText.textContent=t('uidDetectionLabel');toggleLabel.append(toggle,toggleText);const toggleHelp=document.createElement('p');toggleBox.append(toggleLabel,toggleHelp);const pairActions=document.createElement('div');pairActions.className='tm-inline-actions';const createBtn=Object.assign(document.createElement('button'),{className:'secondary'});const updateBtn=Object.assign(document.createElement('button'),{className:'primary'});pairActions.append(createBtn,updateBtn);toggleRow.append(toggleBox,pairActions);const summaryTitle=document.createElement('h3');const lastCheck=document.createElement('div');lastCheck.className='tm-muted';const summaryGrid=document.createElement('div');summaryGrid.className='tm-summary-grid';const pairResultPanel=document.createElement('div');pairSection.append(pairTitle,toggleRow,summaryTitle,lastCheck,summaryGrid,pairResultPanel);const form=document.createElement('div');form.className='tm-regex-bar';const formTitle=document.createElement('header');const titleRow=document.createElement('div');titleRow.className='row';const regexrBtn=Object.assign(document.createElement('button'),{className:'primary'});regexrBtn.style.padding='6px 12px';regexrBtn.style.fontSize='13px';regexrBtn.addEventListener('click',()=>{try{window.open('https://regexr.com/','_blank','noopener');}catch{location.href='https://regexr.com/';}});titleRow.append(formTitle,regexrBtn);const controls=document.createElement('div');controls.className='controls';const patternLabel=document.createElement('label');const patternInput=document.createElement('input');patternInput.type='text';patternInput.style.width='60%';patternInput.placeholder='/^@spam.*/i or ^@promo';const flagsLabel=document.createElement('label');const flagsInput=document.createElement('input');flagsInput.type='text';flagsInput.style.width='80px';flagsInput.placeholder='i';const addBtn=Object.assign(document.createElement('button'),{className:'secondary'});addBtn.style.padding='6px 12px';addBtn.style.fontSize='13px';controls.append(patternLabel,patternInput,flagsLabel,flagsInput,addBtn);form.append(titleRow,controls);const listSection=document.createElement('section');listSection.className='tm-section';const listTitle=document.createElement('h3');const toolbar=document.createElement('div');toolbar.className='tm-toolbar';const topToolbarRow=document.createElement('div');topToolbarRow.className='tm-toolbar-row';const topLeft=document.createElement('div');topLeft.className='tm-toolbar-group';const masterLabel=document.createElement('label');const masterToggle=document.createElement('input');masterToggle.type='checkbox';const masterText=document.createElement('span');masterLabel.append(masterToggle,masterText);const counter=document.createElement('div');counter.className='tm-counter';topLeft.append(masterLabel,counter);const topRight=document.createElement('div');topRight.className='tm-toolbar-group';const typeLabel=document.createElement('label');const searchLabel=document.createElement('label');const searchInput=document.createElement('input');searchInput.type='search';const searchNote=document.createElement('div');searchNote.className='tm-search-note';const typeSelect=document.createElement('select');[['all',''],['handle',''],['id',''],['regex','']].forEach(([value,label])=>{const option=document.createElement('option');option.value=value;option.textContent=label;typeSelect.appendChild(option);});topRight.append(searchLabel,searchInput,typeLabel,typeSelect);topToolbarRow.append(topLeft,topRight);const middleToolbarRow=document.createElement('div');middleToolbarRow.className='tm-toolbar-row';const tagLabel=document.createElement('div');tagLabel.className='tm-counter';const tagGroup=document.createElement('div');tagGroup.className='tm-tag-group';const tagInputs=[];['handle-only','paired','stale','mismatch','unverified'].forEach(code=>{const label=document.createElement('label');const input=document.createElement('input');input.type='checkbox';input.addEventListener('change',()=>{if(input.checked)tagFilters.add(code);else tagFilters.delete(code);invalidateViewState({clearRegex:true});renderList();});const text=document.createElement('span');text.textContent='';label.append(input,text);tagInputs.push({code,input,text});tagGroup.appendChild(label);});middleToolbarRow.append(tagLabel,tagGroup);const bottomToolbarRow=document.createElement('div');bottomToolbarRow.className='tm-toolbar-row';const bulkLeft=document.createElement('div');bulkLeft.className='tm-toolbar-group';const bulkLabel=document.createElement('label');const bulkSelect=document.createElement('select');[['delete',''],['create',''],['update','']].forEach(([value,label])=>{const option=document.createElement('option');option.value=value;option.textContent=label;bulkSelect.appendChild(option);});const executeBtn=Object.assign(document.createElement('button'),{className:'primary'});const clearSelectionBtn=Object.assign(document.createElement('button'),{className:'secondary'});bulkLeft.append(bulkLabel,bulkSelect,executeBtn,clearSelectionBtn);bottomToolbarRow.append(bulkLeft,searchNote);toolbar.append(topToolbarRow,middleToolbarRow,bottomToolbarRow);const list=Object.assign(document.createElement('ul'),{className:'tm-block-list'});listSection.append(listTitle,toolbar,list);wrap.append(versionSection,settingsSection,apiSection,pairSection,form,listSection);const regexMatchCache=new Map();const rowRefs=new Map();let baseViewStateCache=null;let viewStateCache=null;let selectionVersion=0;const getCurrentItems=()=>this.app.storage.all();const getStatusCode=(item,blockedIds=null)=>item.type==='handle'?this.app.pairService.getHandleStatus(item.value,blockedIds).code:null;const markSelectionChanged=()=>{selectionVersion+=1;viewStateCache=null;};const setSelectionValue=(key,selected)=>{if(!key)return false;if(selected){if(selection.has(key))return false;selection.add(key);markSelectionChanged();return true;}if(!selection.has(key))return false;selection.delete(key);markSelectionChanged();return true;};const invalidateViewState=({clearRegex=false}={})=>{baseViewStateCache=null;viewStateCache=null;rowRefs.clear();if(clearRegex)regexMatchCache.clear();};const getItemsRevision=(items)=>(items||[]).map(item=>`${item.type}:${item.value}:${item.flags || ''}`).join('\u001f');const getPairRevision=(items,blockedIds=null)=>(items||[]).filter(item=>item.type==='handle').map(item=>{const status=this.app.pairService.getHandleStatus(item.value,blockedIds);const pair=status?.pair||null;return[getItemKey(item),status?.code||'',pair?.uid||'',pair?.verifiedAt||'',pair?.lastResolvedUid||'',pair?.lastError||'',pair?.source||''].join(':');}).join('\u001e');const pruneSelection=(keyedItems)=>{const valid=keyedItems||new Map(getCurrentItems().map(item=>[getItemKey(item),item]));let changed=false;for(const key of Array.from(selection)){if(!valid.has(key)){selection.delete(key);changed=true;}}if(changed)markSelectionChanged();};const buildBaseViewState=()=>{const allItems=getCurrentItems();const blockedIds=this.app.pairService.getBlockedIdSet(allItems);const keyedItems=new Map(allItems.map(item=>[getItemKey(item),item]));const handleItems=allItems.filter(item=>item.type==='handle');const searchIndex=buildManagerSearchIndex(allItems);const searched=searchManagerIndex(searchIndex,searchQuery);const typeValue=typeSelect.value||'all';const visibleItems=searched.filter(item=>{if(typeValue!=='all'&&item.type!==typeValue)return false;if(!tagFilters.size)return true;if(item.type!=='handle')return false;return tagFilters.has(getStatusCode(item,blockedIds));});const visibleKeys=visibleItems.map(getItemKey).filter(Boolean);return{signature:[getItemsRevision(allItems),getPairRevision(handleItems,blockedIds),String(this.app.settings.isHandleCaseSensitive()),(typeSelect.value||'all'),String(searchQuery||'').trim().toLowerCase(),Array.from(tagFilters).sort().join(',')].join('|'),itemsRevision:getItemsRevision(allItems),allItems,keyedItems,handleItems,blockedIds,visibleItems,visibleKeys,visibleKeySet:new Set(visibleKeys)};};const computeViewState=(force=false)=>{if(force||!baseViewStateCache)baseViewStateCache=buildBaseViewState();pruneSelection(baseViewStateCache.keyedItems);if(!force&&viewStateCache&&viewStateCache.baseSignature===baseViewStateCache.signature&&viewStateCache.selectionVersion===selectionVersion){return viewStateCache;}const selectedItems=Array.from(selection).map(key=>baseViewStateCache.keyedItems.get(key)).filter(Boolean);const selectedHandleCount=selectedItems.filter(item=>item.type==='handle').length;const selectedVisibleCount=baseViewStateCache.visibleKeys.filter(key=>selection.has(key)).length;viewStateCache={...baseViewStateCache,baseSignature:baseViewStateCache.signature,selectionVersion,selectedItems,selectedHandleCount,selectedVisibleCount};return viewStateCache;};const getRegexMatchState=(regexItem,viewState,mode='count')=>{if(!regexItem||regexItem.type!=='regex')return{matchCount:0,matches:mode==='full'?[]:null};const cacheKey=[getItemKey(regexItem),viewState?.itemsRevision||'',String(this.app.settings.isHandleCaseSensitive())].join('|');let entry=regexMatchCache.get(cacheKey);if(!entry){entry={revision:viewState?.itemsRevision||'',caseSensitive:this.app.settings.isHandleCaseSensitive(),matchCount:null,matches:null};regexMatchCache.set(cacheKey,entry);}if(mode==='full'&&Array.isArray(entry.matches))return entry;if(mode==='count'&&typeof entry.matchCount==='number')return entry;const spec=validateRegexSpec(regexItem.value,regexItem.flags||'');if(!spec){entry.matchCount=0;if(mode==='full')entry.matches=[];return entry;}const rx=new RegExp(spec.pattern,spec.flags);if(mode==='full'){const matches=[];for(const item of viewState.handleItems){if(safeRegexTest(rx,item.value))matches.push(item);}entry.matches=matches;entry.matchCount=matches.length;return entry;}let matchCount=0;for(const item of viewState.handleItems){if(safeRegexTest(rx,item.value))matchCount+=1;}entry.matchCount=matchCount;return entry;};const syncVisibleSelection=()=>{for(const[itemKey,refs]of rowRefs.entries()){if(refs?.checkbox)refs.checkbox.checked=selection.has(itemKey);}};const syncApiStatus=()=>{apiInput.value=this.app.apiConfig.getApiKey();apiStatus.textContent=this.app.apiConfig.hasApiKey()?t('apiKeyStatusSaved',this.app.apiConfig.getMaskedApiKey()):t('apiKeyStatusMissing');this._renderApiTestStatus(apiTestStatus,this.app.apiConfig.getLastTestResult(),apiTestBusy);};const syncActionState=(viewState=computeViewState())=>{const hasKey=this.app.apiConfig.hasApiKey();const pairBulk=bulkSelect.value==='create'||bulkSelect.value==='update';createBtn.disabled=busy||!hasKey;updateBtn.disabled=busy||!hasKey;createBtn.textContent=busy?t('pairWorking'):t('pairCreate');updateBtn.textContent=busy?t('pairWorking'):t('pairUpdate');testApiBtn.disabled=apiTestBusy||!hasKey;testApiBtn.textContent=apiTestBusy?t('apiKeyTestRunning'):t('apiKeyTest');masterToggle.disabled=busy||!viewState.visibleKeys.length;masterToggle.checked=!!viewState.visibleKeys.length&&viewState.selectedVisibleCount===viewState.visibleKeys.length;masterToggle.indeterminate=viewState.selectedVisibleCount>0&&viewState.selectedVisibleCount<viewState.visibleKeys.length;executeBtn.disabled=busy||!selection.size||(pairBulk&&(!hasKey||!viewState.selectedHandleCount));clearSelectionBtn.disabled=busy||!selection.size;bulkSelect.disabled=busy;counter.textContent=t('pairResultSummary',{selected:selection.size,visible:viewState.visibleItems.length,total:viewState.allItems.length});};const renderSummary=()=>{const summary=this.app.pairService.getSummary();const cards=[{label:t('pairSummaryHandles'),value:summary.handles},{label:t('pairSummaryPaired'),value:summary.paired},{label:t('pairSummaryNeeded'),value:summary.pairNeeded},{label:t('pairSummaryStale'),value:summary.stale},{label:t('pairSummaryMismatch'),value:summary.mismatch},{label:t('pairSummaryUnverified'),value:summary.unverified}];summaryGrid.replaceChildren(...cards.map(card=>{const box=document.createElement('div');box.className='tm-summary-card';const strong=document.createElement('strong');strong.textContent=String(card.value);const span=document.createElement('span');span.textContent=card.label;box.append(strong,span);return box;}));lastCheck.textContent=t('pairLastCheck',formatDateTime(this.app.pairStore.getLastPairCheckAt()));toggle.checked=this.app.pairStore.isUidDetectionEnabled();caseToggle.checked=this.app.settings.isHandleCaseSensitive();syncApiStatus();this._renderPairResultList(pairResultPanel,this.app.getLastPairRunResult());syncActionState(computeViewState());};const renderList=(viewState=computeViewState())=>{listTitle.textContent=t('manageTitle',viewState.allItems.length);rowRefs.clear();list.replaceChildren();if(!viewState.allItems.length||!viewState.visibleItems.length){const li=document.createElement('li');li.className='tm-list-empty';li.textContent=viewState.allItems.length?(searchQuery?t('searchNoMatches'):t('noFilteredEntries')):t('noEntries');list.appendChild(li);syncActionState(viewState);return;}for(const item of viewState.visibleItems){const itemKey=getItemKey(item);const li=document.createElement('li');const checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.className='tm-item-check';checkbox.checked=selection.has(itemKey);checkbox.addEventListener('change',()=>{setSelectionValue(itemKey,checkbox.checked);syncActionState();});rowRefs.set(itemKey,{checkbox});const left=document.createElement('div');left.className='tm-block-main';const label=document.createElement('div');label.className='tm-block-label';label.textContent=item.type==='regex'?`/${item.value}/${item.flags || ''}`:item.value;const badges=document.createElement('div');badges.className='tm-block-badges';const meta=document.createElement('div');meta.className='tm-block-meta';if(item.type==='handle'){const status=this.app.pairService.getHandleStatus(item.value,viewState.blockedIds);badges.appendChild(this._makeBadge(status.code));if(status.pair?.uid)meta.appendChild(this._createMetaLine(t('metaUid',status.pair.uid)));if(status.pair?.verifiedAt){meta.appendChild(this._createMetaLine(t('metaVerifiedAt',formatDateTime(status.pair.verifiedAt))));}if(status.pair?.lastResolvedUid&&status.pair.lastResolvedUid!==status.pair.uid){meta.appendChild(this._createMetaLine(t('metaResolvedUid',status.pair.lastResolvedUid)));}if(status.pair?.source)meta.appendChild(this._createMetaLine(t('metaSource',status.pair.source)));if(status.pair?.lastError)meta.appendChild(this._createMetaLine(t('metaError',status.pair.lastError)));}else if(item.type==='id'){badges.appendChild(this._makeBadge('uid'));}else if(item.type==='regex'){badges.appendChild(this._makeBadge('regex'));const regexSummary=document.createElement('div');regexSummary.className='tm-regex-summary';const renderRegexSummary=()=>{const currentViewState=computeViewState();const regexActions=document.createElement('div');regexActions.className='tm-regex-actions';const regexState=getRegexMatchState(item,currentViewState,expandedRegexKeys.has(itemKey)?'full':'count');const countLine=document.createElement('div');countLine.className='tm-inline-note';countLine.textContent=t('regexMatchedCount',regexState.matchCount||0);const selectMatchesBtn=Object.assign(document.createElement('button'),{textContent:t('regexSelectMatches')});selectMatchesBtn.disabled=!(regexState.matchCount||0)||busy;selectMatchesBtn.addEventListener('click',()=>{const matchState=getRegexMatchState(item,computeViewState(),'full');let changed=false;for(const match of matchState.matches||[]){const matchKey=getItemKey(match);if(!matchKey||selection.has(matchKey))continue;selection.add(matchKey);changed=true;}if(changed)markSelectionChanged();syncVisibleSelection();syncActionState();Toast.show(t('regexSelectedMatches',matchState.matchCount||0));});const toggleRegexBtn=Object.assign(document.createElement('button'),{textContent:expandedRegexKeys.has(itemKey)?t('regexCollapse'):t('regexExpand')});toggleRegexBtn.disabled=!(regexState.matchCount||0);toggleRegexBtn.addEventListener('click',()=>{if(expandedRegexKeys.has(itemKey)){expandedRegexKeys.delete(itemKey);showAllRegexKeys.delete(itemKey);}else{expandedRegexKeys.add(itemKey);showAllRegexKeys.set(itemKey,REGEX_MATCH_INITIAL_LIMIT);}renderRegexSummary();});regexActions.append(countLine,selectMatchesBtn,toggleRegexBtn);regexSummary.replaceChildren(regexActions);if(!expandedRegexKeys.has(itemKey))return;const fullMatchState=getRegexMatchState(item,currentViewState,'full');const matches=fullMatchState.matches||[];if(!matches.length){const empty=document.createElement('div');empty.className='tm-inline-note';empty.textContent=t('regexNoMatches');regexSummary.appendChild(empty);return;}const listWrap=document.createElement('ul');listWrap.className='tm-regex-match-list';const storedLimit=Number(showAllRegexKeys.get(itemKey));const limit=Math.min(matches.length,Number.isFinite(storedLimit)&&storedLimit>0?storedLimit:REGEX_MATCH_INITIAL_LIMIT);for(const match of matches.slice(0,limit)){const row=document.createElement('li');row.textContent=match.value;listWrap.appendChild(row);}regexSummary.appendChild(listWrap);if(limit<matches.length){const showMoreBtn=Object.assign(document.createElement('button'),{textContent:t('regexShowMore',limit,matches.length)});showMoreBtn.addEventListener('click',()=>{showAllRegexKeys.set(itemKey,Math.min(matches.length,limit+REGEX_MATCH_PAGE_SIZE));renderRegexSummary();});regexSummary.appendChild(showMoreBtn);}else if(matches.length>REGEX_MATCH_INITIAL_LIMIT){const showLessBtn=Object.assign(document.createElement('button'),{textContent:t('regexShowLess')});showLessBtn.addEventListener('click',()=>{showAllRegexKeys.set(itemKey,REGEX_MATCH_INITIAL_LIMIT);renderRegexSummary();});regexSummary.appendChild(showLessBtn);}};renderRegexSummary();meta.appendChild(regexSummary);}const removeBtn=Object.assign(document.createElement('button'),{textContent:t('unblock')});removeBtn.disabled=busy;removeBtn.addEventListener('click',()=>{setSelectionValue(itemKey,false);this.app.removeEntry(item);renderAll();Toast.show(t('removed',label.textContent));});left.append(label,badges);if(meta.childNodes.length)left.appendChild(meta);li.append(checkbox,left,removeBtn);list.appendChild(li);}syncActionState(viewState);};const renderAll=()=>{invalidateViewState({clearRegex:true});renderSummary();renderList();};const applyLanguage=()=>{versionTitle.textContent=t('versionTitle');versionText.textContent=t('versionValue',getScriptVersion());settingsTitle.textContent=t('handleCaseLabel');caseText.textContent=t('handleCaseLabel');caseHelp.textContent=t('handleCaseHelp');caseLegacy.textContent=t('handleCaseLegacy');apiTitle.textContent=t('apiKeyTitle');apiLabel.textContent=t('apiKeyLabel');apiInput.placeholder=t('apiKeyPlaceholder');apiHelp.textContent=t('apiKeyHelp');saveApiBtn.textContent=t('apiKeySave');clearApiBtn.textContent=t('apiKeyClear');pairTitle.textContent=t('uidDetectionLabel');toggleText.textContent=t('uidDetectionLabel');toggleHelp.textContent=t('uidDetectionHelp');createBtn.textContent=t('pairCreate');updateBtn.textContent=t('pairUpdate');summaryTitle.textContent=t('pairSummary');formTitle.textContent=t('addRegex');regexrBtn.textContent=t('testRegex');patternLabel.textContent=t('patternLabel')+':';flagsLabel.textContent=t('flagsLabel')+':';addBtn.textContent=t('addBtn');masterText.textContent=t('selectVisible');searchLabel.textContent=t('searchLabel');searchInput.placeholder=t('searchPlaceholder');typeLabel.textContent=t('typeFilterLabel');typeSelect.options[0].textContent=t('typeAll');typeSelect.options[1].textContent=t('typeHandle');typeSelect.options[2].textContent=t('typeId');typeSelect.options[3].textContent=t('typeRegex');tagLabel.textContent=t('tagFilterLabel');tagInputs.forEach(({code,text})=>{text.textContent=this._makeBadge(code).textContent;});bulkLabel.textContent=t('bulkActionLabel');bulkSelect.options[0].textContent=t('bulkDelete');bulkSelect.options[1].textContent=t('bulkCreatePairs');bulkSelect.options[2].textContent=t('bulkUpdatePairs');executeBtn.textContent=t('execute');clearSelectionBtn.textContent=t('clearSelection');searchNote.textContent=searchQuery?t('searchLabel'):'';renderAll();};const setBusy=(nextBusy)=>{busy=!!nextBusy;renderSummary();};caseToggle.addEventListener('change',()=>{this.app.settings.setHandleCaseSensitive(caseToggle.checked);this.app.refreshAfterStorageChange();renderAll();});toggle.addEventListener('change',()=>{this.app.pairStore.setUidDetectionEnabled(toggle.checked);this.app.refreshAfterStorageChange();renderAll();});searchInput.addEventListener('input',()=>{searchQuery=searchInput.value||'';invalidateViewState({clearRegex:true});renderList();});typeSelect.addEventListener('change',()=>{invalidateViewState({clearRegex:true});renderList();});bulkSelect.addEventListener('change',()=>syncActionState());masterToggle.addEventListener('change',()=>{const viewState=computeViewState();let changed=false;for(const item of viewState.visibleItems){const key=getItemKey(item);if(!key)continue;if(masterToggle.checked){if(selection.has(key))continue;selection.add(key);changed=true;}else if(selection.has(key)){selection.delete(key);changed=true;}}if(changed)markSelectionChanged();syncVisibleSelection();syncActionState();});clearSelectionBtn.addEventListener('click',()=>{if(!selection.size)return;selection.clear();markSelectionChanged();syncVisibleSelection();syncActionState();});saveApiBtn.addEventListener('click',()=>{this.app.apiConfig.setApiKey(apiInput.value);this.app.refreshAfterStorageChange();renderAll();Toast.show(t('apiKeySaved'));});testApiBtn.addEventListener('click',async()=>{apiTestBusy=true;renderSummary();const result=await this.app.testApiKey();apiTestBusy=false;renderSummary();Toast.show(t('apiKeyTestResult',getApiTestCategoryLabel(result.category),result.message,result.httpStatus?String(result.httpStatus):''),3200);});clearApiBtn.addEventListener('click',()=>{this.app.apiConfig.clearApiKey();this.app.refreshAfterStorageChange();renderAll();Toast.show(t('apiKeyCleared'));});createBtn.addEventListener('click',async()=>{setBusy(true);const stats=await this.app.runPairUpdate('create');setBusy(false);Toast.show(t('pairResult',stats),3200);renderAll();});updateBtn.addEventListener('click',async()=>{setBusy(true);const stats=await this.app.runPairUpdate('update');setBusy(false);Toast.show(t('pairResult',stats),3200);renderAll();});executeBtn.addEventListener('click',async()=>{const selectedItems=computeViewState().selectedItems;if(!selectedItems.length)return;if(bulkSelect.value==='delete'){this.app.removeEntries(selectedItems);const removedCount=selectedItems.length;selection.clear();markSelectionChanged();renderAll();Toast.show(t('bulkDeleteResult',removedCount));return;}const handles=selectedItems.filter(item=>item.type==='handle').map(item=>item.value);if(!handles.length){Toast.show(t('bulkHandleRequired'));return;}setBusy(true);const stats=await this.app.runPairUpdate(bulkSelect.value,handles);setBusy(false);Toast.show(t('pairResult',stats),3200);renderAll();});addBtn.addEventListener('click',()=>{let pattern=(patternInput.value||'').trim();let flags=(flagsInput.value||'').trim();if(!pattern)return;const literal=parseRegexLiteral(pattern);if(literal){pattern=literal.pattern;flags=literal.flags||'';}if(!validateRegexSpec(pattern,flags)){Toast.show(t('invalidRegex'));return;}const ok=this.app.storage.addRegex(pattern,flags);if(!ok){Toast.show(t('exists'));return;}this.app.refreshAfterStorageChange();patternInput.value='';flagsInput.value='';renderAll();Toast.show(t('addedRegex'));});applyLanguage();Dialog.show({title:t('manageTitle',this.app.storage.all().length),body:wrap,buttons:[{label:t('import'),value:'import'},{label:t('export'),value:'export'},{label:t('close'),value:false,primary:true}],onRefresh:(ctx)=>{ctx.setTitle(t('manageTitle',this.app.storage.all().length));ctx.buttons[0].textContent=t('import');ctx.buttons[1].textContent=t('export');ctx.buttons[2].textContent=t('close');applyLanguage();}}).then(v=>{if(v==='import')this.importList();else if(v==='export')this.exportList();});}exportList(){const json=JSON.stringify({version:2,exportedAt:Date.now(),items:this.app.storage.all()},null,2);const body=document.createElement('div');const p=document.createElement('p');p.textContent=t('exportHint');const h4a=document.createElement('h4');h4a.textContent=t('json');const ta1=document.createElement('textarea');ta1.readOnly=true;ta1.value=json;const h4b=document.createElement('h4');h4b.textContent=t('text');const ta2=document.createElement('textarea');ta2.readOnly=true;ta2.value=this.app.storage.all().map(it=>it.type==='regex'?exportRegexLiteral(it):it.value).join('\n');body.append(p,h4a,ta1,h4b,ta2);Dialog.show({title:t('export'),body,buttons:[{label:t('close'),value:false,primary:true}],onRefresh:(ctx)=>{ctx.setTitle(t('export'));p.textContent=t('exportHint');h4a.textContent=t('json');h4b.textContent=t('text');ctx.buttons[0].textContent=t('close');}});}importList(){const ta=document.createElement('textarea');ta.placeholder=t('importPlaceholder');Dialog.show({title:t('importTitle'),body:ta,buttons:[{label:t('close'),value:null},{label:t('importBtn'),value:'import',primary:true}],onRefresh:(ctx)=>{ctx.setTitle(t('importTitle'));ta.placeholder=t('importPlaceholder');ctx.buttons[0].textContent=t('close');ctx.buttons[1].textContent=t('importBtn');},onBeforeClose:(val)=>{if(val!=='import')return null;const txt=(ta.value||'').trim();if(!txt)return{ok:false,count:0};let items=[];try{const obj=JSON.parse(txt);if(obj&&Array.isArray(obj.items))items=obj.items;else if(obj&&Array.isArray(obj.handles))items=obj.handles.map(h=>({type:'handle',value:h}));}catch{const parts=txt.split(/\n+/).flatMap(line=>{const trimmed=line.trim();const literal=parseRegexLiteral(trimmed);return literal?[trimmed]:trimmed.split(',');});items=parts.map(s=>s.trim()).filter(Boolean).map(s=>{if(s.startsWith('@'))return{type:'handle',value:s};const literal=parseRegexLiteral(s);if(literal)return{type:'regex',value:literal.pattern,flags:literal.flags};if(isChannelId(s))return{type:'id',value:s};return{type:'handle',value:s};}).filter(Boolean);}const before=this.app.storage.all().length;this.app.storage.setAll([...this.app.storage.all(),...items]);const count=this.app.storage.all().length-before;this.app.refreshAfterStorageChange();return{ok:true,count};}}).then(res=>{if(res&&res.ok)Toast.show(t('importedCount',res.count));});}}class App{constructor(){this.settings=new AppSettingsStorage();this.storage=new StorageV2(this.settings);this.pairStore=new PairMetaStorage(this.settings);this.apiConfig=new ApiConfigStorage();this.pairService=new PairService(this.storage,this.pairStore,this.apiConfig,this.settings);this.hider=new CommentHider(this.storage,this.pairStore,this.settings);this.menu=new MenuEnhancer(this);this.manager=new BlockListManager(this);this._menuCommandIds=[];this._lastPairRunResult=null;this._commentsHost=null;this._commentObserver=null;this._hostObserver=null;this._pageSyncPending=false;this._pageKey=null;this._pairBanner=null;this._bindGlobalEvents();this._bindNavigationEvents();this._syncAcrossTabs();this._registerMenu();this._schedulePageSync();}findHandleRule(handle){return findHandleItem(this.storage.all(),handle,this.settings.isHandleCaseSensitive());}hasHandleRule(handle){return!!this.findHandleRule(handle);}addHandleRule(handle){const ok=this.storage.addHandle(handle);if(ok)this.refreshAfterStorageChange();return ok;}removeHandleRule(handle){const item=this.findHandleRule(handle);if(!item)return false;this.removeEntry(item);return true;}removeEntry(item){if(item.type==='handle')this.pairService.removeHandleArtifacts(item.value);this.storage.remove(item);this.refreshAfterStorageChange();}removeEntries(items){const selectedItems=(items||[]).filter(Boolean);if(!selectedItems.length)return;const removeKeys=new Set(selectedItems.map(getItemKey).filter(Boolean));const handles=selectedItems.filter(item=>item.type==='handle').map(item=>item.value);const artifactIds=this.pairService.collectHandleArtifactIds(handles);this.pairService.removeHandlePairs(handles);this.storage.setAll(this.storage.all().filter(item=>{if(removeKeys.has(getItemKey(item)))return false;return item.type!=='id'||!artifactIds.has(item.value);}));this.refreshAfterStorageChange();}clearAllEntries(){this.storage.clear();this.pairService.clearPairArtifacts();this.refreshAfterStorageChange();}refreshAfterStorageChange(){this.hider.rebuildLookup();const mode=this._getPageMode();const host=this._commentsHost?.isConnected?this._commentsHost:this._findCommentsHost(mode);this.hider.refreshScheduled(host||undefined);this._syncPairBanner();Dialog.refreshAll();}refreshLanguageUi(){this._registerMenu();this._syncPairBanner();Dialog.refreshAll();}getLastPairRunResult(){return this._lastPairRunResult;}showPairResultDialog(stats){this.manager._showPairResultDialog(stats);}async testApiKey(){const result=await this.pairService.testApiKey();this.apiConfig.setLastTestResult(result);this.refreshAfterStorageChange();return result;}async runPairUpdate(mode='update',handles=null){const stats=mode==='create'?(handles?await this.pairService.createPairsForHandles(handles):await this.pairService.createMissingPairs()):(handles?await this.pairService.updatePairsForHandles(handles):await this.pairService.updatePairs({includeMissing:true}));this._lastPairRunResult=stats;this.refreshAfterStorageChange();return stats;}_bindGlobalEvents(){document.addEventListener('contextmenu',(ev)=>{const el=ev.target?.closest?.('#author-text > span, #author-handle, a[href^="/@"]');if(!el)return;let hText=el.textContent?.trim();if(!hText?.startsWith?.('@')){const href=el.getAttribute?.('href');const m=href&&/^\/@([^/?#]+)/.exec(href);if(m)hText='@'+decodeMaybe(m[1]);}const handle=sanitizeHandle(hText);if(!handle)return;const isBlocked=this.hasHandleRule(handle);ev.preventDefault();Dialog.show({title:isBlocked?t('unblock'):t('block'),body:(()=>{const d=document.createElement('div');const p=document.createElement('p');p.textContent=isBlocked?t('confirmUnblock'):t('confirmBlock');const b=document.createElement('b');b.textContent=handle;d.append(p,b);return d;})(),buttons:[{label:t('close'),value:false},{label:isBlocked?t('unblock'):t('block'),value:true,primary:true}],onRefresh:(ctx)=>{ctx.setTitle(isBlocked?t('unblock'):t('block'));ctx.content.querySelector('p').textContent=isBlocked?t('confirmUnblock'):t('confirmBlock');ctx.buttons[0].textContent=t('close');ctx.buttons[1].textContent=isBlocked?t('unblock'):t('block');}}).then(ok=>{if(!ok)return;if(isBlocked){this.removeHandleRule(handle);Toast.show(t('removed',handle));}else{this.addHandleRule(handle);Toast.show(t('added',handle));}});},{capture:true});}_bindNavigationEvents(){const onNavigate=()=>this._schedulePageSync();window.addEventListener('yt-navigate-finish',onNavigate,true);window.addEventListener('popstate',onNavigate,true);document.addEventListener('visibilitychange',()=>{if(!document.hidden)this._schedulePageSync();});}_schedulePageSync(){if(this._pageSyncPending)return;this._pageSyncPending=true;requestAnimationFrame(()=>{this._pageSyncPending=false;this._syncPageState();});}_getPageMode(){const path=location.pathname||'';if(path==='/watch')return'watch';if(/^\/shorts\/[^/]+/.test(path))return'shorts';return'unsupported';}_getPageKey(mode){if(mode==='watch'){const videoId=new URLSearchParams(location.search||'').get('v')||'';return`watch:${videoId}`;}if(mode==='shorts'){const match=/^\/shorts\/([^/?#]+)/.exec(location.pathname||'');return`shorts:${match?.[1] || ''}`;}return`unsupported:${location.pathname || ''}`;}_getPageRoot(mode){if(mode==='watch')return document.querySelector(WATCH_ROOT_SELECTOR)||document.body;if(mode==='shorts')return document.querySelector(SHORTS_ROOT_SELECTOR)||document.body;return null;}_findCommentsHost(mode){if(mode==='watch')return document.querySelector(COMMENTS_HOST_SELECTOR);if(mode==='shorts')return this._findShortsCommentsHost();return null;}_getAncestorChain(node){const chain=[];let current=node;while(current?.nodeType===1){chain.push(current);current=current.parentElement;}return chain;}_findLowestSharedAncestor(nodes){const items=(nodes||[]).filter(node=>node?.nodeType===1);if(!items.length)return null;const firstChain=this._getAncestorChain(items[0]);for(const candidate of firstChain){if(items.every(node=>candidate===node||candidate.contains?.(node)))return candidate;}return null;}_isBroadCommentsHost(node){if(!node||node.nodeType!==1)return true;const tag=(node.tagName||'').toLowerCase();return node===document.body||node===document.documentElement||tag==='ytd-app';}_refineSharedCommentsHost(ancestor,commentRoots){let current=ancestor;const roots=(commentRoots||[]).filter(node=>node?.nodeType===1);if(!current||!roots.length)return null;while(current&&this._isBroadCommentsHost(current)){const nextCandidates=new Set();for(const root of roots){let cursor=root;while(cursor?.parentElement&&cursor.parentElement!==current){cursor=cursor.parentElement;}if(!cursor||cursor.parentElement!==current)return null;nextCandidates.add(cursor);if(nextCandidates.size>1)return null;}current=nextCandidates.values().next().value||null;}return current&&!this._isBroadCommentsHost(current)?current:null;}_findShortsCommentsHost(){const commentNodes=Array.from(document.querySelectorAll(COMMENT_SELECTOR)).filter(node=>node?.isConnected);if(!commentNodes.length)return null;const commentRoots=commentNodes.map(node=>Extractor.getCommentRoot(node)||node).filter(node=>node?.nodeType===1);if(!commentRoots.length)return null;if(commentRoots.length===1){const host=commentRoots[0];return this._isBroadCommentsHost(host)?null:host;}const sharedAncestor=this._findLowestSharedAncestor(commentRoots);if(!sharedAncestor)return null;return this._refineSharedCommentsHost(sharedAncestor,commentRoots);}_disconnectHostObserver(){if(!this._hostObserver)return;this._hostObserver.disconnect();this._hostObserver=null;}_disconnectCommentObserver(){if(this._commentObserver)this._commentObserver.disconnect();this._commentObserver=null;this._commentsHost=null;this.hider.resetObservation();}_watchForCommentsHost(mode,root){if(this._hostObserver||mode==='unsupported')return;if(!root)return;let hostLookupPending=false;let observer=null;observer=new MutationObserver(()=>{if(hostLookupPending)return;hostLookupPending=true;requestAnimationFrame(()=>{hostLookupPending=false;if(this._hostObserver!==observer)return;const currentMode=this._getPageMode();if(currentMode!==mode){this._disconnectHostObserver();return;}const host=this._findCommentsHost(currentMode);if(host)this._attachCommentsHost(host);});});this._hostObserver=observer;observer.observe(root,{childList:true,subtree:true});}_collectRefreshRoots(node,roots){if(node?.nodeType!==1)return;if(node.matches?.(COMMENT_SELECTOR))roots.add(node);const currentRoot=Extractor.getCommentRoot(node);if(currentRoot)roots.add(currentRoot);node.querySelectorAll?.(COMMENT_SELECTOR).forEach(commentNode=>roots.add(commentNode));}_handleCommentMutations(muts){const roots=new Set();const removedRoots=new Set();for(const m of muts){if(m.addedNodes?.length){const targetRoot=Extractor.getCommentRoot(m.target);if(targetRoot)roots.add(targetRoot);for(const node of m.addedNodes)this._collectRefreshRoots(node,roots);}for(const node of m.removedNodes||[])this._collectRefreshRoots(node,removedRoots);}this.hider.unobserveNodes(removedRoots);if(!roots.size)return;this.hider.noteMutationBatch();this.hider.refreshNodes(roots,{invalidate:true});}_attachCommentsHost(host){this._disconnectHostObserver();if(!host)return;if(this._commentsHost!==host||!this._commentObserver){if(this._commentObserver)this._commentObserver.disconnect();this.hider.resetObservation();this._commentsHost=host;this._commentObserver=new MutationObserver(muts=>this._handleCommentMutations(muts));this._commentObserver.observe(host,{childList:true,subtree:true});}this.hider.refreshScheduled(host);}_syncPageState(){const mode=this._getPageMode();const pageKey=this._getPageKey(mode);if(this._pageKey!==pageKey){this._pageKey=pageKey;this.hider.resetTransientState();}if(mode==='unsupported'){this._disconnectHostObserver();this._disconnectCommentObserver();this._syncPairBanner();return;}const host=this._findCommentsHost(mode);if(host)this._attachCommentsHost(host);else{this._disconnectCommentObserver();this._watchForCommentsHost(mode,this._getPageRoot(mode));}this._syncPairBanner();}_syncAcrossTabs(){if(typeof GM_addValueChangeListener==='function'){GM_addValueChangeListener('blocked_v2',(_k,_old,val,remote)=>{if(!remote)return;if(val&&val.version===2&&Array.isArray(val.items)){this.storage.setAllLocal(val.items);this.refreshAfterStorageChange();Toast.show(t('syncToast'));}});GM_addValueChangeListener('pair_meta_v1',(_k,_old,val,remote)=>{if(!remote)return;this.pairStore.setAllLocal(val);this.refreshAfterStorageChange();Toast.show(t('pairSyncToast'));});GM_addValueChangeListener('youtube_data_api_v3_config',(_k,_old,val,remote)=>{if(!remote)return;this.apiConfig.setAllLocal(val);this.refreshAfterStorageChange();});GM_addValueChangeListener('app_settings_v1',(_k,_old,val,remote)=>{if(!remote)return;this.settings.setAllLocal(val);this.refreshAfterStorageChange();});GM_addValueChangeListener('lang',(_k,_old,_val,remote)=>{if(!remote)return;this.refreshLanguageUi();});}}_syncPairBanner(){if(this._getPageMode()!=='watch'||!this.pairService.shouldNotify()){this._pairBanner?.remove();this._pairBanner=null;return;}const summary=this.pairService.getSummary();if(!this._pairBanner){const banner=document.createElement('div');banner.className='tm-banner';const title=document.createElement('strong');const body=document.createElement('p');const actions=document.createElement('div');actions.className='actions';const updateBtn=Object.assign(document.createElement('button'),{className:'primary'});updateBtn.dataset.role='update';const laterBtn=Object.assign(document.createElement('button'),{className:'secondary'});laterBtn.dataset.role='later';updateBtn.addEventListener('click',async()=>{updateBtn.disabled=true;laterBtn.disabled=true;updateBtn.textContent=t('pairWorking');const stats=await this.runPairUpdate('update');Toast.show(t('pairResult',stats),3200);this.showPairResultDialog(stats);this._syncPairBanner();});laterBtn.addEventListener('click',()=>{this.pairService.dismissNotification();this._syncPairBanner();});actions.append(updateBtn,laterBtn);banner.append(title,body,actions);document.body.appendChild(banner);this._pairBanner=banner;}this._pairBanner.querySelector('strong').textContent=t('pairBannerTitle');this._pairBanner.querySelector('p').textContent=t('pairBannerBody',summary.stale,summary.mismatch);this._pairBanner.querySelector('[data-role="update"]').textContent=t('updateNow');this._pairBanner.querySelector('[data-role="later"]').textContent=t('later');}_registerMenu(){try{if(typeof GM_unregisterMenuCommand==='function'){for(const id of this._menuCommandIds.splice(0)){try{GM_unregisterMenuCommand(id);}catch{}}}const manageId=GM_registerMenuCommand(t('menuManage')||'Manage',()=>this.manager.openList());const clearId=GM_registerMenuCommand(t('menuClear')||'Clear',()=>{Dialog.show({title:t('clear')||'Reset',body:(()=>{const p=document.createElement('p');p.textContent=t('confirmClear')||'Reset all blocked entries?';return p;})(),buttons:[{label:t('close')||'Close',value:false},{label:t('clear')||'Reset',value:true,primary:true}],onRefresh:(ctx)=>{ctx.setTitle(t('clear')||'Reset');ctx.content.firstChild.textContent=t('confirmClear')||'Reset all blocked entries?';ctx.buttons[0].textContent=t('close')||'Close';ctx.buttons[1].textContent=t('clear')||'Reset';}}).then(ok=>{if(!ok)return;this.clearAllEntries();Toast.show(t('clear')||'Reset');});});const langId=GM_registerMenuCommand('🌐 Language: '+getLang().toUpperCase(),()=>{const next=getLang()==='ko'?'en':'ko';try{GM_setValue('lang',next);}catch{}Toast.show('Lang: '+next.toUpperCase());this.refreshLanguageUi();});this._menuCommandIds=[manageId,clearId,langId].filter(Boolean);}catch{}}}if(TEST_HOOK&&typeof TEST_HOOK==='object'){Object.assign(TEST_HOOK,{AppSettingsStorage,StorageV2,PairMetaStorage,ApiConfigStorage,PairService,Dialog,BlockListManager,App,t,validateRegexSpec,parseRegexLiteral,exportRegexLiteral,safeRegexTest,buildManagerSearchIndex,searchManagerIndex,getScriptVersion});}if(!TEST_HOOK?.skipBootstrap){requestAnimationFrame(()=>new App());}})();
