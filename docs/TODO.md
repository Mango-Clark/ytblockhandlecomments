# TODO

## P0 — Critical

## P1 — High

## P2 — Normal

- [ ] (A) `설정 - 설정 - 매칭` 위에 margin을 약간 추가

  - (1) 설정 화면의 해당 영역 간격만 조정해 UI/UX를 개선.

- [ ] (B) `설정 - 설정 - 키워드 자동 처리`의 각 선택사항 설명 추가

  - (1) 각 선택사항의 동작과 적용 범위를 설정 UI에서 자세히 설명.

- [ ] (C) `설정 - 설정 - 유지보수`의 설정 초기화 버튼 경고 강조

  - (1) 설정 초기화 버튼을 빨간색으로 표시해 파괴적 동작임을 명확히 알림.

- [ ] (D) 차단 및 키워드 자동 처리 통합 탭 추가

  - (1) `차단된 채널 - 정규식 추가`와 `설정 - 설정 - 키워드 자동 처리`를 포함하는 탭 추가.
  - (2) 통합 탭에서 각 기능 탭으로 이동하는 버튼 추가.
  - (3) 각 기능 탭에서 통합 탭으로 이동하는 버튼 추가.
  - (4) `설정 - 설정 - 키워드 자동 처리`에는 전체 총괄 실행 옵션만 남기고, 나머지 선택사항은 통합 탭으로 이관.

- [ ] (E) 테마 설정 확장

  - (1) 설정에서 다음 모드를 선택할 수 있도록 지원: `라이트`, `다크`, `기기설정`, `기기설정(반대)`, `yt설정`, `yt설정(inverted)`, `커스텀`.
  - (2) `기기설정`은 OS의 `prefers-color-scheme`을 따르고, `기기설정(반대)`는 그 결과를 반전.
  - (3) `yt설정`은 현재 YouTube 테마를 감지해 적용하고, `yt설정(inverted)`는 감지한 YouTube 테마를 반전.
  - (4) 테마 모드 변경 시 설정 화면과 모든 관리 UI에 즉시 적용하고, 새로고침/탭 재실행 후에도 저장된 모드를 복원.
  - (5) `커스텀` 선택 시 별도의 테마 커스텀 UI 창을 열어 색상 등 사용자 값을 편집하고 저장할 수 있게 구현.
  - (6) 커스텀 UI는 각 모드의 적용 범위를 명확히 보여주고, 기본값 복원 및 잘못된 색상값 검증을 제공.
  - (7) YouTube 본문/기존 YouTube UI와 확장 UI의 테마 적용 범위를 분리해 기존 페이지 스타일을 오염시키지 않도록 구현.

## P3 — Low

## Blocked

## Backlog

## Done

- [x] (A) 댓글 `⋯` 메뉴에 `Hide comments from this channel` 항목 추가
- [x] (B) verbose level 설정 추가
- [x] (C) 내보내기 창에 뒤로가기 버튼 추가
- [x] (D) 내보내기 창에 파일로 내보내기 버튼 추가
